#!/bin/sh
# Переходник. Настоящий сборщик живёт в sources/ — он знает, где корень репозитория,
# и складывает дерево в sources/build/. Этот файл остался от более раннего шага и
# однажды уже сработал ловушкой: он считал корень на уровень выше и падал с
# «Cores missing» , указывая на несуществующий путь.
exec "$(cd "$(dirname "$0")" && pwd)/sources/assemble.sh" "$@"
