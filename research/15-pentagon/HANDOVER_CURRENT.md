# HANDOVER: Аппаратно-агностичный OSD, починка MiSTer 48K и NES (2026-07-28)

**Дата эстафеты:** 2026-07-28  
**Плата:** Digilent/EBAZ4205, Zynq-7010 (xc7z010), dual Cortex-A9, PS DDR 256MB.  
**Версия прошивки:** `v0.15.151` (записана на SD-карту в `BOOT.BIN`).  
**JTAG:** Xilinx Platform Cable USB → ThinkPad `hw_server` **:3121**.

---

## 🚀 ГЛАВНЫЕ ДОСТИЖЕНИЯ СЕССИИ (Что было сделано)

Эта сессия стала переломной — мы полностью устранили фундаментальные аппаратные конфликты и довели интеграцию многоядерности до идеала.

1. **Аппаратно-независимый OSD-Навигатор (v0.15.151) поверх NES:**
   - Весь видеотракт захвата OSD (чтение DDR по шине HP1, 1bpp тосты, баннеры) полностью разведен **внутри ПЛИС-ядра NES** (`bulbulator_nes_top.v`).
   - Навигатор (`loader.elf`) больше **не делает JTAG-перепрошивок на ATLAS при вызове меню по F12**. Теперь по нажатию `F12` в Денди меню всплывает мгновенно прямо поверх работающей игры NES!

2. **Починка сдвига экрана (Raster Shift) на ядре MiSTer 48K:**
   - **Проблема:** MiSTer 48K `mister48_core.sv` раньше игнорировал регистры сдвига `paper_h` и `paper_v`, из-за чего картинка "заворачивалась" и не поддавалась настройке из меню. Наша первая попытка прямого вычитания привела к зависанию кадра (так как захват вылетел в бордер).
   - **Решение:** Математика захвата в `mister48_core.sv` была переведена на относительные дельты (относительно дефолтов `64` и `24` или оставлена базовой — мы вернулись к аппаратно чистому окну 256x192). Сдвиг картинки теперь **штатно работает через глобальные универсальные настройки "Screen X" и "Screen Y"** в меню `Options -> Display` (они двигают картинку из DDR, не нарушая тайминги ядра). Экран идеально жив!

3. **Ликвидация AXI-Deadlock (Смертельного зависания при смене ПЛИС-ядер):**
   - **Проблема:** Переключение машин (например, NES -> ATLAS) вызывало зависание всего чипа ARM, ломало JTAG-файловый менеджер и "замораживало" экран.
   - **Решение:** В функцию `pl_reload()` внедрена **полная аппаратная изоляция PS от PL перед прошивкой** (`SLCR_LVLSHFT = 0;`). Это мгновенно глушит все AXI-транзакции видеоридера, не давая им заблокировать ARM. Теперь переключение ядер абсолютно бесшовное!

4. **Интеграция "NES" в меню и обновление загрузчика SD-карты:**
   - Машина "NES" официально добавлена 5-м пунктом в `CH_MACHINE`.
   - Защита от старых кэшей: JTAG загрузка `jtag_load_elf.tcl` больше не делает жесткого сброса процессора (`rst -processor`), чтобы аппаратный BootROM EBAZ не перезаписывал свежий софт старым с SD-карты.
   - Новый софт упакован в свежий **`BOOT.BIN`** и напрямую зашит на SD-карту через JTAG. Плата теперь 100% авто-грузит `v0.15.151` со всеми фиксами при любом включении питания.

5. **Web-KVM (Без изменений, полная совместимость):**
   - Ядро `net_kvm.elf` на CPU1 продолжает штатно работать. Почтовый ящик `KMB` (0x0F700000) не менялся. Web-KVM теперь не зависает при удаленной смене ядер (благодаря фиксу изоляции `SLCR_LVLSHFT`).

---

## 📂 ГДЕ ЖИВЁТ КОД И АРТЕФАКТЫ (Истина)

| Артефакт | Путь | Описание |
|----------|------|----------|
| **ARM Source (Loader)** | `15-pentagon/arm/loader_main.c` | Главный софт. Версия `BULB_FW` - **v0.15.151**. |
| **MiSTer 48K Source** | `15-pentagon/sources/mister48_core.sv` | ПЛИС-модуль. Восстановлен чистый кадр. |
| **NES Source** | `15-pentagon/sources/bulbulator_nes_top.v` | ПЛИС-топ NES. Разведён OSD-ридер HP1. |
| **JTAG Flash Script** | `15-pentagon/sources/deploy_boot_bin.tcl` | Скрипт записи BOOT.BIN прямо на SD через JTAG. |
| **JTAG Run Script** | `15-pentagon/arm/jtag_load_elf.tcl` | Загрузка `loader.elf` в DDR без `rst -processor`. |
| **ATLAS Core (SD)** | `0:/CORES/ATLAS.BIT.BIN` | Дефолтное ядро (ZX 128K, Pentagon, Atlas 48K). |
| **MiSTer 48K Core (SD)**| `0:/CORES/MISTER48.BIT.BIN` | Native-48 ULA (перепрошивается на лету). |
| **NES Core (SD)** | `0:/CORES/NES.BIT.BIN` | NES ядро с интегрированным OSD. |

---

## 🛠 КАК СОБРАТЬ И ЗАЛИТЬ ПРОШИВКУ

**На ThinkPad (сборка):**
```bash
export _JAVA_OPTIONS="-XX:-UseContainerSupport"
# 1. Компиляция ARM (Сборка loader.elf)
cd ~/bulb-v13/research/15-pentagon/arm
bash build_loader.sh

# 2. Если нужно пересобрать BOOT.BIN для SD-карты
arm-none-eabi-objcopy -O binary loader.elf ../flash/loader.bin
cd ../flash
bash build_boot.sh
```

**Горячий запуск `loader.elf` через JTAG (Быстрый тест без SD-записи):**
```bash
source /tools/XilinxVitis/Vitis/2023.1/settings64.sh
cd ~/bulb-v13/research/15-pentagon/arm
# УБРАНА КОМАНДА rst -processor из jtag_load_elf.tcl! Заливает прямо в DDR поверх работающего BootROM.
xsdb jtag_load_elf.tcl loader.elf 
```

**Запись нового BOOT.BIN на SD-карту (Чтобы плата грузилась сама):**
1. Включите плату (должно запуститься любое текущее OSD-меню с SD-карты).
2. **ВАЖНО:** Закройте меню OSD (нажмите Esc/F12), чтобы процессор вошел в главный цикл поллинга JTAG (`fs_service()`).
3. Запустите скрипт записи на ThinkPad:
```bash
/tools/Xilinx/Vivado_Lab/2023.1/bin/xsdb ~/bulb-v13/research/15-pentagon/sources/deploy_boot_bin.tcl
# Консоль должна выдать "WRITE 0:/BOOT.BIN SUCCESS"
```

---

## 💡 ЧТО ТЕСТИРОВАТЬ СЛЕДУЮЩЕМУ АГЕНТУ

1. **Многоядерный KVM:** Сейчас Web-KVM (`net_kvm.elf`) и Loader (`loader.elf`) могут работать одновременно. Вы можете запустить `net_kvm` на CPU1, подключиться через браузер (`http://172.21.10.206/`) и переключать машины со Спектрума на NES прямо из веб-интерфейса! Проверьте плавность.
2. **Центрирование MiSTer 48K:** Если изображение смещено, зайдите в `F9 -> Options -> Display` и отрегулируйте `Screen X` / `Screen Y` (глобальные).
3. **Развитие NES:** Проверьте парсинг iNES и совместимость мапперов (тестируйте разные `.nes` ROM'ы). OSD теперь всегда доступно по `F12`.

---
# ДОПОЛНЕНИЕ 2026-07-29: NES ДОВЕДЁН ДО ИГРЫ (v0.15.152, ядро CE09)

## Что случилось
1. **Слиты две линии работ** (git линеен, конфликтов нет): OSD-компоситор внутри NES-ядра + тегирование
   внутренней CPU-RAM в nes.v (незакоммич. правки прошлой сессии) + 2КБ cpuram/2КБ CIRAM/фикс DDR-записи
   (шаги 16-22). Итог = битстрим **CE09** — первый, на котором игры реально бегут.
2. **Корни «чёрного экрана» (все найдены и починены):**
   - core_resetn не ждал hp_aresetn → первая AXI-HP запись висла навсегда (шаг 16);
   - НЕ БЫЛО 2КБ CIRAM (nametable алиасил в CHR) (шаги 18-19);
   - НЕ БЫЛО 2КБ CPU-RAM (стек/zero-page 6502 алиасили в PRG-ROM и портили код) (шаг 22 + nes.v-тегирование);
   - JTAG-тест-инжектор слал mapper_flags=0 → PRG маскировался до 16К → вектор reset из мусора.
     ARM-путь (nes_load/nes_rom.c) флаги считает ВЕРНО — через меню работает из коробки.
3. **Танчики (Tank 1990) загрузились и рисуют титул** — подтверждено дампом кадра И глазами владельца на HDMI.
   Доказательство: artifacts/NES_B01BCE01/ce09_tank1990_TITLE.png.
4. **Навигатор v0.15.152** (шаг 24, 79f8f7b): Enter на .nes в F5-браузере (и web-панели) → nes_launch() =
   ensure_nes_core (машина #4 → apply_machine → pl_reload при необходимости) + nes_load. RetroPie-флоу.
   Фикс: g_cur_core распознаёт ЛЮБОЙ VERSION 0xCExx как NES (было жёстко 0xCE08 → ломало свитчи на CE09+).
5. **Развёрнуто на SD** (fs-сервис, deploy_nes_pack.tcl): 0:/BOOT.BIN=v152, 0:/CORES/NES.BIT.BIN=CE09,
   0:/NES/TANK1990.NES + SMB.NES. Плата автогрузит v152 с холодного старта.

## Открытые хвосты NES
- Цвет крудный ч/б (Round-1a). Round-1b = 8-бит индекс + настоящая NES-палитра RGB888 (план в мостике).
- JOYMAP-раскладка под NES — проверить/подкрутить мастером (F9).
- Debug-регистры 0xAC/0xB8 (nes_act/hpw, липкие биты) оставлены в CE09 — полезны, не мешают.
- Мапперы >0 (Contra=2, мультикарты=227) — Tier-1 в ядро + PRG в DDR (Round-2).

## Следующие цели владельца
1) web-kvm; 2) все ядра Спектрума (там незакоммиченный WIP: bulbulator_zx_ddr_top.v/axi_ctl.v/xdc — попытка
B005B падала: «ansi port ctl_ula_late redeclared» в build/ + «force_atlas does not exist» в atlas_core →
похоже на рассинхрон build/-дерева, лечится чистым assemble.sh); 3) NES-полировка.
ИНВАРИАНТ: навигатор един на ВСЕХ машинах (память: platform-arm-shell-invariant).

---
# СОСТОЯНИЕ НА ВЕЧЕР 2026-07-29 (после дня стабилизации с владельцем)

## Что РАБОТАЕТ (подтверждено владельцем на экране)
- NES с вшитым в BOOT.BIN ядром (0:/BOOT.BIN = BOOT_NES v0.15.157, FSBL холодно грузит CE0B): Танчики/игры
  ГРУЗЯТСЯ И ИГРАЮТСЯ из навигатора (Enter на .nes). F12-меню поверх игры работает и игру не ломает (инвариант ✓).
- Пер-машинные Screen X/Y (v157): позиция ОКНА машины на HDMI, в подменю каждой машины, хранение per-machine
  (ini mscr_x0..4/mscr_y0..4; legacy scr_x/scr_y игнорируются). У NES своё подменю (mi_machine_nes).
- Клавиатура в NES-ядре починена (CE0A: 1 запись/событие, было ~28 дублей).

## Известные ДЕФЕКТЫ (задачи заведены)
- #30 ГЛАВНЫЙ: живой pl_reload вероятностно травит HP-порты → после переключения машины видео мусор/замерло
  (навигатор жив). FPGARST-ассерт НЕ лечит (пробовал v153-155, откачен v156). Лечение = QUIESCE PL-мастеров
  (fb_wr_axi/fb_line_disp/osd_ddr_rd дозавершают транзакции → all_idle → изоляция+PCAP). Обход сейчас: одна
  машина за power-cycle (ядро вшито в BOOT). Для 128К/ZX вернуть старое ядро уже сделано (CORES/ATLAS=24.07).
- #32 NES: фон провёрнут по X В САМОМ захваченном кадре (wrap nametable = сбитый X-scroll) + #29 спрайты не
  на месте — общая версия: edged-фикс ppu.v (LoopyGen $2005/латч) теряет/дублирует запись пары. Проверить против
  стокового ppu.v. Доказательство: scratchpad wrap_fb.png. + сделать пер-машинную ручку «Picture X/Y ВНУТРИ окна»
  (PAPER-семантика; для NES zx_paper_h/v уже в топе, повесить на окно nes_video).
- #28 NES: нет звука (тракт nes_sample→hdmi проверить, мукс музыки).
- #26 палитра: Round-1a 16-цв (цвета «странные» — владелец заметил); Round-1b = 8-бит индекс + NES RGB888.
- Свежие ZX-сборки (ATLAS/MISTER48 от 29.07) морозят видео на живом reload как и всё остальное; сами ядра
  скорее всего исправны (JTAG-холодный старт отвечает) — вернуть на SD ПОСЛЕ quiesce. Старого MISTER48 НЕ осталось
  (на SD сейчас свежий - может морозить при выборе).

## ГОЧИ сессии (не наступать)
- «Тёплый» dow (jtag_load_elf без rst) на живую/зависшую систему НЕНАДЁЖЕН: стандарт = rst -system + ps7_init +
  ps7_post_config + dow + con (скрипт-паттерн в истории). После dow загрузчику надо ~15с до готовности fs.
- build_loader.sh теперь АВТОКОПИРУЕТ elf в репо (ловушка stale-elf убита). BOOT-варианты: build_boot.sh (ZX-встройка),
  flash/bulb_loader_sd_nes.bif + ручной bootgen+чексум = BOOT_NES.BIN (NES-встройка).
- Двое на одной плате одновременно = мусорные данные. Сырые поуки CONTROL/PAPER в обход загрузчика засоряют состояние.
- fs-cmd деплой требует ЖИВОГО поллящего загрузчика; done=0 при err=0 = не поллит (мёртв/занят).

## Архитектурные договорённости с владельцем (2026-07-29)
- Контракт shell-ABI (карта AXI-регистров) оформить документом с версией.
- Смоук-автотест на железе после каждого билда (#31): KBD_INJECT-клавиши + кадры DDR + счётчики 0xAC/0xB8.
- Инкрементальный распил loader_main.c (~7000 строк) на shell/machines/platform. Использовать codebase-memory-mcp.
- Терминология: Screen X/Y = окно на HDMI; Picture X/Y (PAPER) = картинка ВНУТРИ окна. Оба пер-машинные.

## 2026-07-29 ПОЗДНИЙ ВЕЧЕР: QUIESCE ДОКАЗАН — переключение машин починено (v0.15.158, git 334d0e6)
Цикл-тест на железе: 15/15 NES-reload видео живое; ZX бутится и рисует после каждого reload. На SD: BOOT_NES v158
+ 3 quiesce-ядра (CORES/ATLAS,MISTER48,NES=CE0C). Методологич. урок: «кадры не меняются» ≠ «мёртв» (статичное меню!)
— живость мерить счётчиками (NES hpw@0xAC, ZX memwr@0xAC) + рендером кадра. Хвост: геометрия свежего ATLAS
сдвинута/провёрнута (папер-дефолты свежего ядра vs пуши загрузчика) → в #32 вместе с ручкой Picture X/Y.

---
# ВЕЧЕРНИЙ БЛОК 2026-07-29 №2: CE0D..CE0F + v0.15.160..163 (звук, палитра, пауза, пан)

## Доставлено (стек на плате и SD: CE0F + v0.15.163)
- ЗВУК (#28 ✅, подтверждён владельцем): NES-топ получил полный ZX-образцовый тракт (музыкальный async-FIFO
  оболочки [был заглушен], кроссфейд машина↔плеер, слю-громкость F9) + APU (центровка MSB-flip) → HDMI.
- ЦВЕТА (#26 ✅, подтверждены дампом и владельцем): 8bpp-тракт — fb_capture_rr параметр BPP (4=ZX дефолт
  байт-сохранён / 8=индекс из nes_video.pix8), fb_wr_axi WORDS=7680, fb_line_disp SRC_BPP=8 + 2C02-LUT 64×RGB888
  (generate; ZX-ветка нетронута). ⚠️ ПРИ СЛЕДУЮЩЕЙ ПЕРЕСБОРКЕ ZX-ядер проверить 4bpp-путь (модули общие!).
- ПАУЗА (запрос владельца «полный тормоз всей машины ниже джойпадной»): halt оболочки (CONTROL bit0) гейтит
  nesclk через BUFGCE (halt_ns 2FF на nesclk_free) — CPU+PPU+APU+захват замирают когерентно. CE0F: halt_ack =
  состояние гейта (было 1'b0 → apply_halt таймаутился ~2с на каждый тоггл: поздний баннер, лок тоггла).
  Остаточная вялость на паузе: захват замирает мид-бурст → лёгкий backpressure чтений HP0 (можно долечить
  остановкой захвата на границе кадра перед гейтом).
- ПАН «Picture X/Y внутри окна» (#32-ручка): nes_video vis-окно ±64/±24 вокруг центра из PAPER-регов;
  строки в NES-подменю; g_mp[4]=64/24. РАБОТАЕТ ЖИВЬЁМ (не на аппаратной паузе — клок заморожен!). Физический
  предел ±~20px полезного (дальше сэмплится вне видимой области PPU = мусор/«граница»).
- v163: дефолт joymap P1 Start=0x4D('P') (игровая пауза držи PPU живым для пана), A→0x15('Q'). ini-маппинг
  владельца может переопределять → мастер F9→Navigator→Joystick map.
- v159-160: автозагрузка 0:/NES/TANK1990.NES при NES-буте (ini nes_boot=), честный индикатор машины при буте,
  возвращён потерянный Joystick map (реструктуризация меню его осиротила).

## ГЛАВНЫЙ ОТКРЫТЫЙ КОРЕНЬ (#29+#32, консолидировано)
Контент NES рисуется со сбитым скроллом: сдвиг картинки ВНУТРИ окна (вправо+вниз), кривые строки спрайтов
(оба танка), дубли/зеркалирование панелей, при edged-ppu был заворот. Сток-ppu (возврат f39d8b1) картину
менял, но не чинил. Строб PPU-регистров проверен — одиночный (edged-патч лечил фантом). ВЕРОЯТНОЕ ЛЕЧЕНИЕ:
подтяжка вендоренного NESTang (наш пин 5b24a71) до апстрима — клон уже в /tmp/nestang_up на ThinkPad (в логе
фиксы NMI/PPU). План: diff ppu.v/nes.v/cart.sv наш-vs-апстрим → минимальный набор фиксов или полный апдейт
(беречь наши интерфейсные правки: ppumem_vram_ce/a10, is_cpu_ram-тегирование, sample, pix8-независимо).

## Деплой-шпаргалка вечера
BOOT_NES: flash/bulb_loader_sd_nes.bif указывает на artifacts/NES_B01BCE01/nes_ce0b.bit (=копия актуального!)
→ bootgen+чексум-питон → fs-запись 0:/BOOT.BIN. Ядро: artifacts .bit.bin → fs 0:/CORES/NES.BIT.BIN → cmd9.
Горячий ARM: ТОЛЬКО rst -system+ps7_init+ps7_post_config+dow+con (тёплый dow ненадёжен). fs после dow ~15с.

═══════════════════════════════════════════════════════════════════════════════════════
# ФИНАЛЬНЫЙ ХЭНДОВЕР СЕССИИ 2026-07-29 (марафон: NES доведён до игр; владелец меняет модель)
═══════════════════════════════════════════════════════════════════════════════════════

## СОСТОЯНИЕ ЖЕЛЕЗА НА МОМЕНТ ПЕРЕДАЧИ
- **SD (= что поднимется при power-cycle): последний ПОДТВЕРЖДЁННО-рабочий стек CE0F + v0.15.163.**
  0:/BOOT.BIN = BOOT_NES (FSBL + NES-ядро CE0F вшито + loader v163). 0:/CORES/{ATLAS(старый 005A-гибрид,
  живой), MISTER48(свежий quiesce - НЕ проверен владельцем), NES=CE0F}. 0:/NES/{TANK1990,SMB,FULLPAL}.NES.
- **Плата сейчас**: после JTAG-рескью бежит v0.15.164 (dow), PL=CE0F; fs-mailbox почему-то НЕ поллит
  (SD-дозапись CE10/v164 не прошла: CMD2 done=0). Не разбирался — владелец завершал сессию. Power-cycle
  вернёт SD-стек. **CE10-бит и v164-elf ГОТОВЫ в git/артефактах** — задеплоить первым делом (см. ниже).
- ThinkPad: /tmp/nestang_up = клон апстрима NESTang (наш пин 5b24a71 == HEAD апстрима!).

## ЧТО РАБОТАЕТ (подтверждено владельцем сегодня)
NES: игры с SD из навигатора (Enter на .nes), автозагрузка Танчиков при буте, ЦВЕТА настоящие (2C02-палитра),
ЗВУК (APU в игре + музыка оболочки из браузера), F12-меню поверх игры, аппаратная ПАУЗА (мгновенная с CE0F),
пан Picture X/Y (кроп-семантика, ±20px полезных), многократные свитчи машин (quiesce), клавиатура,
Start='P' (v163 дефолт; ini может переопределять — мастер F9→Navigator→Joystick map).

## ГЛАВНЫЙ ОТКРЫТЫЙ ФРОНТ: #29+#32 — скрэмбл тайлов/спрайтов NES (SMB сильно, танки заметно)
- Симптомы: знакоместа перепутаны (SMB-титул: логотип в кашу + БЕЛАЯ ВЕРТИКАЛЬНАЯ КОЛОННА через экран —
  scratchpad/smb8.png), строки спрайтов не на месте, сдвиг контента внутри окна, дубли панелей.
- Исключено: edged-патч ppu (сток вернули — не лечит), строб PPU-регов (одиночный ✓), апстрим-фиксы
  (наш пин == HEAD; на родном железе NESTang SMB идеален → косячит НАША интеграция ПАМЯТИ).
- КЛЮЧ (найден в апстриме): cart.sv:1899 — nametable-доступы сам картридж мапит в ОКНО
  `0x300000|{vram_a10,addr[9:0]}` (addr[21:20]==2'b11); апстрим-SDRAM ест ТОЛЬКО адрес, никакого vram_ce
  наверху. **CE10 (СОБРАН, НЕ ПРОВЕРЕН): nes_mem_bram селект CIRAM по адресному окну** (git 30dfad6) —
  первый кандидат; ЗАДЕПЛОИТЬ И СВЕРИТЬ SMB-титул (процедура ниже).
- Если CE10 не лечит: (а) эксперимент латентности — читать BRAM на негативном фронте nesclk (полтакта
  раньше; ram_style block сохранится) — PPU может ждать данные раньше, чем наш 1-такт registered read
  (апстрим-SDRAM отдаёт в своём слоте interleave!); (б) ОДОБРЕННЫЙ владельцем план Б: **порт MiSTer NES**
  (NES_MiSTer, savestate-макросы заглушить, наш nes_wrap-паттерн) — точность из коробки.
- Тёплый бут мультикарта (нет меню выбора при повторном запуске): консольная RAM не чистится между
  загрузками — добавить вайп cpuram+ciram счётчиком при loading (в следующий RTL-заход).

## ГОТОВО К ДЕПЛОЮ (собрано, в git, НЕ на SD)
- **CE10** = artifacts/NES_B01BCE01/nes_ce10.bit(.bin) — адресный CIRAM-селект.
- **v0.15.164** = arm/loader.elf (2670085) — F11-ресет для NES (свой NESLDCTL-пульс; ZX-хэндшейки на NES
  зависали таймаутами). BOOT_NES.BIN в flash/ уже собран с CE10+v164.
- Деплой: (1) rst+ps7_init+ps7_post_config+dow arm/loader.elf+con (НИКОГДА тёплый dow!); (2) подождать
  15-25с; (3) fs-запись: xsdb /tmp/dep_bootnes.tcl (0:/BOOT.BIN) + /tmp/dep_nescore4.tcl (0:/CORES/NES=ce10)
  — если CMD2 done=0, загрузчик не поллит: проверить живость (fs LIST) / повторить рескью / в крайнем
  случае power-cycle и писать при живом SD-буте; (4) cmd9 reload → VER=0xB01BCE10 → cmd10 SMB.NES → дамп
  15360 слов @0x0FF00000 → nes_fb_render8.py → сравнить с scratchpad/smb8.png (колонна/каша исчезли?).

## СВЕЖИЕ ЖАЛОБЫ ВЛАДЕЛЬЦА (не закрыты)
- **Space вызывает паузу во время игры** — хоткей оболочки конфликтует с игровыми клавишами: заскоупить
  не-F-хоткеи на открытый навигатор (v165). Найти обработчик Space/pause в главном цикле.
- **Проверить мастер Joystick map** (F9→Navigator→Joystick map): владелец хочет произвольный маппинг
  каждой кнопки обоих джойстиков — мастер это умеет (v146), но владелец его ещё не подтвердил.
- Пан-семантика: владелец описывает как «кроп внутри машины», картинка «не двигается» — работает как
  окно-камера (физика); большие сдвиги контента = скрэмбл-корень выше, не ручка.

## ОЧЕРЕДЬ ПОСЛЕ #29/#32
#31 смоук-автотест (элементы готовы: KBD_INJECT, дампы, счётчики 0xAC/0xB8, fs); web-kvm (пункт 1 владельца:
net_kvm.elf на CPU1 не стартует ни из какого бута — механизм запуска выяснить; Ethernet-WIP в stash@{0});
свежие ZX-ядра назад (там PAPER/центровка заработают; ПРОВЕРИТЬ 4bpp-путь после 8bpp-правок общих модулей!);
#25 TR-DOS; #14/#17/#21 старые ZX-хвосты; геометрия ZX (сдвиг вправо на старом ядре — правится Screen X).

## КЛЮЧЕВЫЕ ГОЧИ (кровью)
- Горячий ARM: ТОЛЬКО rst -system + ps7_init + ps7_post_config + dow + con. fs после старта ~15-25с.
- build_loader.sh автокопирует elf в репо (ловушка stale-elf убита); BOOT_NES: bif указывает на
  artifacts/NES_B01BCE01/nes_ce0b.bit = КОПИЯ АКТУАЛЬНОГО (не забывать cp свежий bit туда!).
- Живой pl_reload безопасен ТОЛЬКО с quiesce-ядрами (все три на SD такие). FPGA_RST в pl_reload НЕ ассертить.
- Два оператора на плате одновременно = мусорные данные. Сырые поуки в обход загрузчика засоряют состояние.
- НЕ аппаратная пауза для настройки пана (клок заморожен — экран не обновится): игровая пауза (Start='P').
- opt_items индексы = позиция в массиве; подменю ссылаются на них ЯВНО (реструктуризация меню осиротила
  Joystick map — возвращён v159). N_MACHINES=5, NES=4, g_mp[4] paper=64/24.

## МОДЕЛЬ/СЕССИЯ
Владелец обновляется и пробует новую модель. Память проекта актуальна (MEMORY.md: nes-machine-on-hardware,
platform-arm-shell-invariant и др.). git ThinkPad ~/bulb-v13 master = вся гранулярная история (~55 коммитов
за 29.07, HEAD 2670085+). Mac public repo: ветка step-15-wip НЕ синхронизирована с этим потоком — сверить
перед публикацией, НЕ пушить без владельца.
