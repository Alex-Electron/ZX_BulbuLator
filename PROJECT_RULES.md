# BulbuLator / EBAZ4205 — правила проекта

> Этот файл — свод постоянных правил и «гоч», каждая из которых оплачена временем: ложным
> диагнозом, испорченным файлом или сорванной сборкой. Он писался как рабочая памятка, а не
> как документация, поэтому тон резкий и местами повторяющийся — так надёжнее. Ссылки вида
> `файл:строка` указывают на исходники в этом же репозитории.

## Где что живёт

| Что | Где |
|-----|-----|
| **Истина по коду** | ThinkPad `~/bulb-v13`, ветка `master` (гранулярная история) |
| Mac-репозиторий | `BulbuLator/`, ветка `step-15-wip` — **отстаёт**, не пушить без владельца |
| Хэндовер (читать первым) | `research/15-pentagon/HANDOVER_CURRENT.md` |
| Сборка, прошивка, JTAG | только ThinkPad: `ssh thinkpad`, `hw_server` на `:3121` |
| **Тестовый софт и дампы** | `~/Yandex.Disk.localized/DIY/ZX/ZX-Soft/` (в т.ч. `tests/`, `Border/`, `Sinclair/`) |
| Заливка на SD по JTAG | ThinkPad: `research/15-pentagon/tools/put_files.tcl` и `put_big.tcl` |

## 🥇 Точность машин: спорное — в опцию, а не в спор (задание владельца 12.08.2026)

Приоритет номер один — **довести Пентагон до идеала**, то есть убрать расхождения с настоящей машиной.
Правило на все такие работы:

**Если эталоны противоречат друг другу — не выбирать за владельца, а выносить ПЕРЕКЛЮЧАТЕЛЬ в настройки
машины.** И обязательно записывать, ГДЕ и В КАКОЙ СИТУАЦИИ каждая опция нужна: опция без объяснения,
когда её трогать, бесполезна и хуже отсутствия опции.

Живой пример: порт `#FF` у Пентагона. У нас `atlas_core/main.v:883` отдаёт `0xFF` (вслед за MiSTer
`mZX ? ff_data : 8'hFF`), а Sizif-512 `cpld/rtl/video.sv:255-260` отдаёт **атрибут**. Правильного
ответа «вообще» нет — клоны Пентагона расходятся между собой, и софт бывает написан под оба. Значит
это опция машины, а не решение агента.

**Куда класть:** слово `MACHINE_CFG` (`0xBC`). Заняты биты 0-2 (модель, фаза ULA), 4 (снег), 8 (трап
TR-DOS), 9 (сервисная страница), 10, 11-12 (гейт SAA), 13 (NMI), 14-16 (объём ОЗУ). **Биты 0..6
занимать НЕЛЬЗЯ** — у NES там region/palette/sprlimit того же слова. Свободны **17..31**.

**Эталоны для сверки** (правило «сверяться с ЖИВЫМ апстримом» действует и здесь):
- Sizif-512 — `github.com/UzixLS/zx-sizif-512`, каталог `cpld/rtl/` (Pentagon/128/48/+3e, ULAplus,
  всё влезает в CPLD на 1270 макроячеек, то есть по ресурсам он нам не помеха);
- MiSTer ZX-Spectrum — `cores/zx-mister/rtl/`, но копия СТАРШЕ живого апстрима.

## Гочи по железу (оплачены временем)

- **🥇 `stop` по JTAG БЕЗ гарантированного `con` = «оболочка зависла» на ровном месте (оплачено ложным
  диагнозом 20.08).** Мой диагностический скрипт остановил Cortex-A9, упал на неверной команде и до
  `con` не дошёл — процессор остался стоять. Машина в ПЛИС продолжала работать (она тактируется сама),
  поэтому картина была ровно как у зависшей прошивки: интерфейс не отзывается, мейлбокс молчит,
  счётчики главного цикла не растут, а звук и видео живы. Признак, отличающий это от настоящего
  отказа: `stop` отвечает **`Already stopped`**. Правила: состояние читать НЕРАЗРУШАЮЩЕ (`mrd -force`
  работает на живом ядре, останавливать незачем); если `stop` всё же нужен — только `catch {stop}` и
  `con` в конце скрипта, включая пути с ошибкой. Не путать с полуоткрытым бёрстом в S_AXI_HP0
  (замёрзший кадр при живом звуке) — там нужен настоящий POR, это другой отказ.
- **🥇 БУФЕР МЕЙЛБОКСА `FS_BUF (0x0F900000)` — ОБЩИЙ С ФАЙЛОВОЙ СЛУЖБОЙ ПРОШИВКИ; данные в нём живут
  до первой же её операции (оплачено испорченным `BOOT.BIN` и незагружающейся платой 20.08).**
  `dow` образа идёт минуты, потом команда записи может ждать свободного интерфейса ещё минуты — и за
  это время прошивка читает в ТОТ ЖЕ буфер свой файл. У меня это был набор ПЗУ (ровно 65536 Б,
  читается при сбросе машины и смене набора): на карту легли 64 КБ кода Z80 плюс остаток образа.
  **Размер файла правильный, `done=1`, `n` = полный размер — и при этом файл негодный.** BootROM
  такой заголовок не принимает: плата не грузится И запирает отладочный доступ (ядра видны как
  `Running`, но память по JTAG не читается вовсе, включая SLCR; лечится только холодным старом, а
  дальше — только кардридером). Правила: заливать `tools/put_retry.tcl` (он сверяет буфер по точкам,
  включая НАЧАЛО, перед КАЖДОЙ попыткой, а после записи читает файл обратно командой 12 и сверяет);
  никогда не считать буфер своим, пока команда не принята; размер сверять листингом каталога.
  **Признак этой порчи:** первые 65536 байт не совпадают, дальше файл верен; начало выглядит как
  `f3 c3 …` (`DI : JP` — код Z80), а не как таблица векторов ARM (`fe ff ff ea …`).
- **Каждое окно оболочки обязано сохранять свой фон в СТЕКЕ (`box_push`/`box_pop`), исключений быть
  не должно.** Слоты, расписанные по договорённости, дважды дали артефакты (подменю, оставшееся на
  фоне; обрывки настроек от визарда), а окно копирования, закрывавшееся полной перерисовкой канвы,
  дало «окно исчезло, а прогрессбар бежал» — через раз, потому что это вопрос момента. Сторож:
  `tools/audit_boxes.py` (запускать после добавления любого окна). Диагностика на плате — счётчики
  `g_bst_leak` (каркас починил сам), `g_bst_over` (глубже стека), `g_bst_under` (лишнее закрытие).
- **Мейлбокс молчит, пока владелец работает в интерфейсе, и это НЕ поломка.** `fs_service` зовётся
  только из главного цикла, а открытое меню или модальное окно до него не доходят (bg_pump под
  модальным намеренно не трогает файловую службу). Одиночная попытка заливки в такой момент даёт
  `done=0, err=0` — то же, что при настоящем отказе. Заливать `tools/put_retry.tcl`: данные уходят
  ОДИН раз (`dow` больших образов идёт минуты, терять их нельзя), а команда повторяется, пока
  прошивка её не возьмёт. Перед долгой заливкой просить владельца выйти в навигатор.
- **НИКОГДА не делать `rst -system` на живой плате.** Работающий PL продолжает писать в S_AXI_HP0,
  пока PS в сбросе → полуоткрытый бёрст залипает в PS-овом AFI-FIFO → `hpw` (0x43C000AC, старшие 16 бит)
  застревает на 1, кадр в DDR замерзает при живом звуке. Не лечится ни PCAP-перепрошивкой, ни
  `FPGA_RST_CTRL`, ни level-shifter'ами — **только холодный старт (POR)**.
- Смена ядра — штатным `pl_reload` (fs cmd 9): он делает QUIESCE (`GP0+0x114`, ждёт STATUS bit3) и
  изоляцию `SLCR_LVLSHFT=0`. QUIESCE спасает только пока PL-мастера живы.
- Горячий ARM по JTAG: **только** `rst -system` + `ps7_init` + `ps7_post_config` + `dow` + `con`
  (тёплый `dow` ненадёжен) — и помнить про гочу выше.
- **Ввод NES:** `joypad_clock` (`nes.v:387`) — это УРОВЕНЬ на весь такт чтения CPU, а не импульс.
  Сдвигать регистр джойпада **только по спаду** (как `nestang_top.sv:452`). Сдвиг по фронту = процессор
  получает уже сдвинутое значение: все кнопки съезжают на бит, а 8-е чтение отдаёт вдвинутую `1'b1` =
  фантомная зажатая кнопка (в меню мультикарта = вечное листание). Исправлено в CE20.
- **Нельзя проверять ввод по SMB после титульника** — аттракт-демо само ведёт Марио вправо по 1-1
  с обычным HUD. Проверять тем, чего в демо не бывает: Select на титуле (курсор 1P↔2P) или пауза.
- fs-мейлбокс `0x0F700000`: `cmd` +4, `done` +8, `err` +C, `len` +10, `n` +14, путь +0x200, путь2 +0x400,
  вывод +0x800, буфер `0x0F900000`.
  Команды: 1 LIST, 2 WRITE, 3 DELETE, 4 RENAME, 5 MKDIR, 6 COPY, 7 APPEND, 9 PL RELOAD, 10 NES LOAD,
  11 KEY INJECT (обслуживается даже из модального ожидания клавиши), **12 READ файла → буфер** (v0.15.170), 13 WRITE-AT (v0.15.428), **14 FRAME SNAPSHOT (v0.15.435): три буфера кадра машины → FS_BUF с шагом 0x10000 за ~3 мс** — единственный честный способ получить ОДИН кадр по JTAG: прямое чтение `0x0FF00000` длится ~15 с и даёт смесь сотен кадров (тройной буфер вращает роли каждый кадр), а `CONTROL bit0 HALT` морозит только процессор, не ULA. Инструмент `snap14.py`.
- **🥇 `mrd -force -value` ОТДАЁТ ДЕСЯТИЧНОЕ ЧИСЛО, а не шестнадцатеричное (оплачено 18.09 ложным
  «в ПЛИС не B0196»).** Разбор вывода через `int(x, 16)` даёт правдоподобный мусор: `2954559894` —
  это `0xB01B0196`, а не `0x2954559893`. Признак промаха: значение версии длиннее 32 бит или
  `MACHINE_CFG` не похож на известный. Разбирать `int(x, 10) & 0xFFFFFFFF`.
- **🥇 ПУТЬ В МЕЙЛБОКС ПИСАТЬ ТОЛЬКО БАЙТАМИ (`mwr -force -size b`), и это НЕ косметика.** По умолчанию
  `mwr` пишет СЛОВО; запись слова по невыровненному адресу xsdb округляет к выровненному, поэтому все
  символы ложатся в одно слово и в буфер уходит ПУСТОЙ путь. Пустой путь FatFs читает как корень —
  значит сломанный инструмент «работает» на `0:/` и падает с `err=5` (FR_NO_PATH) на КАЖДОМ
  подкаталоге. Образец верной записи — `proc wstr` в `tools/put_retry.tcl`.
- **Сборки ядер живут в `/tmp/bNNNN-build/`** (самодостаточная копия `sources/build` со ссылками на
  `cores/zx` и `cores/hdmi`), а НЕ в `sources/build` — там дерево от 11.09. Заводить новую так:
  `cp -a /tmp/b<прошлая>-build /tmp/b<новая>-build`, снести `*.bit *.log *.jou`, положить правленые
  файлы, `vivado -mode batch -source build.tcl`. ⚠ **`research/15-pentagon/assemble.sh` — ОСТАТОК ОТ
  ШАГА 13 и не работает** (считает корень репозитория на уровень выше, падает «Cores missing. Run:
  /home/<пользователь>/get_deps.sh»). Боевой — `research/15-pentagon/sources/assemble.sh`. И помнить:
  он делает `rm -rf build`, то есть затирает дерево сборки.
- **Регистры дисплея только на запись:** `SCR_POS` (0xD0) и `SCR_SCALE` (0x118) читаются как `DEADBEEF`.
  Живые значения смотреть по символам ELF: `arm-none-eabi-nm loader.elf | grep opt_mscr` → `mrd` по адресу.
  Адреса символов **сдвигаются при каждой сборке** — брать из свежего elf, иначе прочитаешь мусор.
- **fs cmd 12 (READ): `len` — это ЗАПРОШЕННЫЙ ЛИМИТ, а не размер файла** (в прошивке: «up to g_fs_len
  bytes»). Не выставил — прочитаешь столько, сколько осталось в поле с прошлой операции, и получишь
  ОБРЕЗАННЫЙ файл; возвращаемый `len` тоже равен лимиту, а не числу прочитанных байт. Перед чтением
  писать `len` = 65536 (или 0 — тогда прошивка сама возьмёт 65536). **Сколько байт прочитано
  ФАКТИЧЕСКИ — лежит в поле `n` (+0x14)**, гадать по содержимому не нужно (это уточнение снято
  советом 02.08; именно гадание испортило `bulbulator.ini` 31.07: прочитал 630 Б из 2 КБ и записал
  обрезок назад).
- ❌ **ОТМЕНЕНО 02.08: «`err` не обнуляется при успехе» — ЭТО НЕВЕРНО.** Прошивка ставит `g_fs_err = 0`
  в начале КАЖДОЙ команды (`loader_main.c:1463`, с 25.07). Я записал эту гочу 31.07 по ложному следу:
  «пропавшие» 20 файлов на самом деле записались, а ложный FAIL давал таймаут поллинга в 45 с на
  многомегабайтной записи — то есть лечится увеличением таймаута по размеру, а не обнулением `err`.
  Обнулять самому не вредно, но искать причину надо в таймауте и в гонке на `done`.
- **🥇 Состояние клавиатуры НЕ проверять на плате после тёплой перезагрузки.** Клавиатура питается
  независимо от ПЛИС (свои 4k7 и +5 В), поэтому `rst -system` / загрузка с карты перезапускают плату,
  но НЕ её — лампы и внутреннее состояние переживают перезагрузку. Симптом (07.08): NumLock «срабатывал
  только со второго раза»; после ХОЛОДНОГО старта лампа загорелась сама и всё пошло с первого нажатия,
  то есть дефекта в прошивке не было. Сам переключатель (`loader_main.c:9438`) верен, и стартовое
  зеркалирование лампы из ini уже сделано в v0.15.215. Правило: любые странности ввода сначала
  перепроверять НА ХОЛОДНОМ СТАРТЕ, иначе диагноз ставится по чужому состоянию. (Тем же может
  объясняться и невоспроизведённый отказ загрузки TAP от 06.08 — не доказано, но правдоподобно.)
- **Имя файла в мейлбоксе и в браузере обрезается на 96 символах (`NAMELEN`), МОЛЧА.** Файл при этом
  записывается с ПОЛНЫМ именем и читается по нему нормально (проверено: имя 99 символов, cmd 12 отдаёт
  ровно те же байты), но в листинге (cmd 1) видно только 96 знаков — у TOSEC-имён отрезает как раз
  расширение, и вывод выглядит как «файла нет». Я на этом уже построил ложный диагноз. Следствие: длинные
  имена **непригодны в навигаторе** (открывать он будет обрезанное имя) — такие файлы переименовывать
  (cmd 4) при заливке. Не путать с `FF_MAX_LFN`: у FatFs предел 255, обрезает НАШ буфер.
- **Правка ini с хоста только целиком и с проверками:** склеивать «прочитанное + своя строка» нельзя —
  обрезок без перевода строки слипается с твоим ключом (`pent10\0\0region=2`, прошивка законно
  игнорирует). Разбирать в ключ=значение, отбрасывать строки без `=` и байты вне ASCII, проверять
  число ключей и размер, писать целиком, читать обратно и сверять байт-в-байт.
- **🥇 ПОСЛЕ ЗАГРУЗКИ С КАРТЫ НАВИГАТОР УЖЕ ОТКРЫТ (`showbrowser at boot` = YES по умолчанию), а канва
  `0x0F800000` после тёплой перезагрузки НЕСВЕЖАЯ — показывает прошлый сеанс.** Оплачено 11.09: «первый F12
  не срабатывает» — на деле он ЗАКРЫВАЛ навигатор, стрелки уходили в машину, а снимок канвы врал. Состояние
  оболочки читать по её переменным через `arm-none-eabi-nm loader.elf` + `mrd`: `browser_on`, `osd_on`,
  `bcursor`, `fcount`, имя под курсором `flist[bcursor]` (шаг `NAMELEN+1`, адрес выравнивать по 4 — `mrd -bin`
  с невыровненного адреса отдаёт пустоту). Инструменты на ThinkPad: `/tmp/nav.py <клавиши>` (нажать и доложить
  состояние), `/tmp/open_nav.py` (открыть навигатор с проверкой, не жать F12 вслепую).
- **Не инжектить клавиши (cmd 11), пока владелец работает в интерфейсе:** в режиме захвата визарда
  маппинга ЛЮБАЯ клавиша = назначение кнопки, то есть инжект молча перепишет его настройку.
- **С ядра CE28 кадр клавиатуры собирает ФАБРИКА, а не ARM.** `KBD_DATA` бит10 = «расширенная» (была
  последовательность `E0`), префиксы `E0`/`F0` наружу не идут (`E1` идёт — на нём матчер Pause), а
  байты-ответы устройства (`FA` ACK, `AA` BAT, `EE`, `FE`) в FIFO скан-кодов не попадают вовсе: они
  уходят в ящик `0x13C`. Признак умения — `LOAD_CAPS` бит3; прошивка держит старый программный путь
  для ядер без него, поэтому **кэш возможностей обязан сбрасываться при каждой смене ядра**.
- **`0x13C` = собственный регистр диагностики PS/2 оболочки** (машино-независимый):
  `{счётчик ответов[31:24], последний ответ[23:16], ошибки чётности[15:0]}`. Читать PS/2-диагностику
  ТОЛЬКО отсюда. `0xB8` остаётся сырым машинно-зависимым словом (на NES там отладка памяти ядра) —
  на нём уже дважды строились неверные выводы.
- **Инжект (cmd 11) идёт В ОБХОД свёртки префиксов PS/2:** `kbd_data_read` отдаёт значение из очереди
  ДО разбора `E0`/`F0`. Инжектить надо уже свёрнутый код (`0xF4` = стрелка вправо), а не пару
  `E0`+`0x74` — иначе в таблицу зажатых лягут две отдельные «клавиши» `E0` и `74`. Следствие:
  **свёртку префиксов инжектом проверить нельзя в принципе**, только настоящей клавиатурой.
- **cmd 10 (NES LOAD) = `nes_load()`, а не `nes_launch()`:** он только стримит ром в BRAM и НЕ отдаёт
  экран машине (`OSD_CTRL&=~3; osd_on=browser_on=0`) и **не чистит ОЗУ консоли**. При открытом
  навигаторе `joymap_eval` законно держит джойстик в нуле — со стороны выглядит как «игра висит,
  Start не работает». Состояние прошлого запуска (режим 2 игроков, позиция курсора) переживает
  перезагрузку рома. Чистый старт = `cmd 9` (PCAP-перезагрузка ядра) → `cmd 10`.
- **Ввод проверять только тем, чего не делает аттракт-демо.** «Кадр изменился» не доказывает ничего:
  у Tank 1990 за 1.5 с покоя БЕЗ ввода накопилось 8219 различающихся байт. Доказывает курсор меню
  (Select двигает его на строку — видно как сдвиг диапазона строк в дампе кадра).
- **Кадр ZX в DDR — 4bpp, 384×302** (у NES 8bpp, 256×240): один байт = ДВА пикселя, палитра ZX 16 цветов.
  Общий адрес тот же `0x0FF00000`. Перепутать форматы = «каша» вместо картинки.
- **ini пишется ТОЛЬКО ручным «Save config»** (единственный вызов `config_save`) — ручной сейв владельца
  перетирает правки хоста. Править конфиг с хоста: cmd 12 (прочитать) → патч → cmd 2 (записать).
- **FatFs регистронезависим:** `smb.nes` откроет `SMB.NES`. Имена ромов на карте сверять листингом
  (cmd 1), а не додумывать — иначе ложный `err=0xC3` (bad `NES\x1A`) на «отсутствующем» файле.
  Мейлбокс молчит, если процессор не дошёл до главного цикла (открытое OSD-меню, крэш) — проверять
  `state`/`PC` через xsdb: `Xil_ExceptionNullHandler` = ARM упал в исключение.
- Диагностика видео: `0x43C000AC` = `{hpw, nes_act}` (hpw растёт = запись в DDR доходит),
  `0x43C000B8` = счётчик nametable-записей + sticky-биты. Кадр NES: 61440 Б (256×240, 8bpp) @ `0x0FF00000`.
- **🥇 ДВА ФАЙЛА С ОДНИМ ИМЕНЕМ МОДУЛЯ = МОЛЧАЛИВАЯ ПОДМЕНА (оплачено немым SAA1099 много сборок).**
  В `build.tcl` читались и настоящий `zx/src/saa1099.sv`, и `zx/src/saa.v` — ЗАГЛУШКА с тем же именем
  (`out_l=out_r=0`). Побеждает прочитанный ПОЗЖЕ, и звук был выведен в ноль. Лог не молчал:
  `CRITICAL WARNING [Synth 8-9873] overwriting previous definition of module` плюс у всех портов чипа
  `unconnected or has no load`. **После КАЖДОЙ сборки грепать лог на `8-9873` и на `has no load` у
  портов.** Правку общего файла из `cores/` (его копируют шаги 06-14) делать только форком в
  `sources/` (образец — `turbosound_bulb.v`) И выкидывать апстримный из списка чтения, иначе получится
  ровно эта же подмена.
- **🥇 «Софт ведёт себя странно» — СНАЧАЛА проверить режим машины, а не железо.** «Щелчки и обрывы в
  SAA-тесте» и «ETUNES показывает квадраты и висит» объяснялись тем, что машина стояла в **ZX 128K**,
  а софту нужен мегабайт (ETUNES пишет это своим же скроллом). Приборный признак: `MACHINE_CFG` бит0 = 0
  и в `MACH_DBG` «запросов в DDR» = 0. A/B двух ядер снял ложный след до того, как я начал его копать.
- **SAA1099 сидит на `#FF`, где A8 выбирает регистр:** `#01FF` = адресный, `#00FF` = данные (проверено
  по коду ETUNES: `OUT #01FF,#1C` затем `OUT #00FF,#02` = регистр 0x1C Sound enable/Reset). Тот же
  младший байт — системный регистр Beta Disk, поэтому порт делится ПО ВРЕМЕНИ: опция машины
  `MACHINE_CFG [12:11]` AUTO/ON/OFF, и AUTO глушит SAA только при вставленной странице ПЗУ TR-DOS ИЛИ
  живой активности дисковода. **Глушить по липкой `bdi_session` НЕЛЬЗЯ** — она держится до сброса, и
  любой SAA-пак, загруженный с дискеты, замолчал бы на весь сеанс. Чтения `#FF` конфликта не имеют:
  SAA write-only. И **`wd_prepare` в признак активности брать нельзя**: при `EDSK=0` это `img_mounted`,
  уровень на всё время вставленного образа.
- **SAA1099 хочет РОВНО 8 МГц** (на них сходится и формула тона `f = 15625·2^oct/(511−freq)`). Деление
  `spclk 56.6667/7` давало 8.0952 = +20.3 цента на всём. Сделано фазовым аккумулятором. Сам чип
  (`saa1099.sv`, Sorgelig по SAASound) сверен с datasheet в xsim и ВЕРЕН — стенд `tb_saa1099.sv`.
- **Селект чипа TurboSound — только значения `#F8..#FF`** (`d[7:3]` все единицы), как в эталоне
  `cores/zx-mister/rtl/turbosound.sv`. Условие «есть бит `d[4]`» ловило лишнее: ETUNES делает
  `OUT #FFFD,#F6` и уводил второй AY без ведома программы.
- **`0x170 AUD_DBG`** — пики звука по источникам: `{SAA[31:24], AY2[23:16], AY1[15:8], SpecDrum[7:2],
  бипер[1]}`, окно 74 мс. Правило владельца: **не объявлять музыку рабочей без пик-метра.** Ноль на
  тишине ничего не доказывает — нужен положительный отсчёт.
- **TS-FM у нас ЕСТЬ, и это YM2203 (OPN), а не OPL3:** `cores/zx-mister/rtl/turbosound.sv` +
  дерево `jt12/` с `jt03.v`. Не влезает по LUT — сначала пункт бэклога «буферы оболочки LUTRAM→BRAM».
- **Формат SCL** (проверен на 400 образах): `"SINCLAIR"`, байт числа файлов N, N записей по 14 байт
  (имя[8], тип, адрес[2], длина[2], **секторов**[1]), затем данные подряд, хвост 0/4/8 байт.
  Число секторов брать ТОЛЬКО из каталога — по размеру файла проверять нельзя, часть образов имеет
  лишний хвост. SCL = дефрагментированный TRD: каталог синтезируется, данные читаются из файла.
- **Страничность Пентагона 1024:** `банк[5:0] = {7FFD[5], 7FFD[7], 7FFD[6], 7FFD[2:0]}`. Расширенные
  биты — ОТДЕЛЬНАЯ защёлка: уход в стандартный режим (`EFF7[2]=1`) её НЕ обнуляет. Бит 5 блокирует
  страничность только при `EFF7[2]=1`. `EFF7` сбрасывается в 0 → машина стартует в мегабайтном режиме;
  его декодирование неполное (`A15..A12=1110`, `A3=0`). Банки 0..7 в BRAM, 8..63 в PS DDR (`0x0FE00000`).
- **Такты ожидания памяти вставлять в `pc3M5`/`nc3M5` (обе фазы), а НЕ в `cpu_ten`:** `cpu_ten` — только
  экспорт наружу для тейпового счётчика, T80 тактируется от `pc3M5`/`nc3M5`. Проверка правильности:
  `VGEOM` старшие 12 бит остаются 320 при активной работе с DDR (стоит процессор, а не ядро).
- **🥇 CDC-правило (оплачено битой памятью Пентагона, ядро B0074): нагрузку ДЕРЖАТЬ, синхронизировать
  ТОЛЬКО флаг, и флаг отдавать НА ТАКТ-ДВА ПОЗЖЕ данных.** Если данные и тоггл меняются одним фронтом, а
  у флага 3 триггера против конвейера данных из 2, у флага остаётся ОДИН такт запаса — совпадение фронтов
  (окно ~0.5 нс на 10 нс ≈ 5 %) его съедает, и приёмник берёт значение ПРЕДЫДУЩЕЙ транзакции. Так
  машина получала чужой байт в 5-10 % чтений расширенных банков; измеренный перекос на пути запроса был
  0.572 нс при шаге фазовой сетки 0.588 нс. Эталон приёма — `inject_cdc.v` (нагрузка держится, берётся
  напрямую). Однотактовый импульс запроса ещё и ЗАЩЁЛКИВАТЬ: потерянный вне `S_IDLE`, он оставлял
  `mwait` поднятым и морозил Z80 насмерть.
- **🥇 Приёмку памяти машины проходит САМА МАШИНА, а не ARM.** В приёмке Пентагона 1024 писала машина, а
  читал и сверял ARM — у ARM-овской стороны мастера CDC нет вовсе, поэтому она была чиста, а машинный
  путь чтения не проверялся ни разу. И читать надо ВСЕ восемь байтовых линий (два крайних байта банка —
  это линии 0 и 7) в темпе настоящего софта (`PUSH`/`LDIR`), а не по одному байту с паузами.
- **Тест, который затирает банк со своим же кодом, даёт ложные числа.** Инжектируемый код живёт в банке 2
  (окно 0x8000) — заливать банк 2 нельзя, иначе программа сходит с рельсов и печатает «ошибки» на
  исправном железе (у меня дважды: «56 расхождений в каждом банке»). UMT по той же причине щадит банк 5.
- **`MEM_STAT` (0x14C) до B0074 собирался из 34 бит в 32-битный провод** — старшие два срезались молча,
  и все поля читались со сдвигом на 2. Расшифровки диагностики до B0074 недостоверны.
- **Мина: окно памяти машины `0x0FE00000` лежит ВНУТРИ 6-МБ диапазона fs-буфера (`FS_BUF 0x0F900000`).**
  Файл больше ~5 МБ, прочитанный мейлбоксом, затрёт расширенные банки Пентагона.
- **Тест памяти без ЗАТРАВКИ не доказывает ничего:** если записать то же значение, что уже лежит,
  «прочитал верно» не отличить от «прочитал старое». Перед каждым прогоном писать заведомо другой байт.
  Оплачено ложным «отказом», который потом не воспроизвёлся.
- **`0x150 MACH_DBG`** — машино-агностичный слот отладки (оболочка даёт слот, машина наполняет).
  ZX/Пентагон: `{запросов в DDR[31:24], EFF7[21:14], 7FFD[11:6], банк[5:0]}`. Счётчик запросов
  8-битный и НАСЫЩАЕТСЯ на 255 — дельту по нему после этого не мерить.
- **Исполнить свой код на Z80 по JTAG** (без файлов и без набора клавиш): `CONTROL=4` (сброс+очистка,
  ждать ~2 с) → `CONTROL=1` (HALT) → `RAMADDR 0x10` = адрес, байты в `RAMDATA 0x14` (автоинкремент) →
  `P7FFD 0x3C` + `PFE 0x40` + `COMMIT 0x44 = 1` → слова `DIR0..DIR6` (`0x20..0x38`) + `COMMIT = 2` →
  `CONTROL=0`. Раскладка DIR — как в `inject_finish` прошивки. RAM-адрес 17 бит = `банк*16384 + смещение`.
- **Читать ТЕКСТ с экрана машины:** зеркало экрана по JTAG (`mrd -bin -file 0x40008000 1728`), клетки
  8×8 сверить со шрифтом ПЗУ (`rom128.hex`, смещение `0x7D00`) — видно, какое меню открыто. Инжект в
  матрицу Z80 — регистр `0xA8` `{отпускание(бит8), скан-код}`, он минует гейт OSD. В 128-меню курсор
  двигает скан-код `0x72` (ядро мапит его в CS+6), а клавиша «6» — НЕ двигает.
- **ПЗУ ГРУЗИТСЯ С КАРТЫ (ядро ZX B0073, прошивка v0.15.207).** 4 страницы по 16 КБ в BRAM, вторым
  портом их пишет ARM. **Каноническая раскладка задана НАМИ: 0 = 128-меню, 1 = 48 BASIC, 2 = TR-DOS,
  3 = сервисное.** Порядок страниц В ФАЙЛЕ любой — ARM раскладывает ПО СОДЕРЖИМОМУ (у пентагоновских
  BIOS порядок `[сервис, TR-DOS, 128, 48]`). Наборы лежат в `0:/ROMS/`, выбор — опция машины `ROM SET`,
  ini-ключи `<тег>.romset` (строка!) и `<тег>.svcrom`.
  - Регистры: `0x154` ROM_LD (байт + addr++), `0x158` ROM_LDCTL (bit0 loading = машина в СБРОСЕ,
    bit3 rewind), `0x15C` ROM_LDADDR (R/W), **`0x144` ROM_LDCNT = сколько байт ФАКТИЧЕСКИ легло в BRAM**.
    Сверять заливку по `0x144`, а НЕ по адресу: адрес — намерение, счётчик — факт.
  - `LOAD_CAPS` (0xC0) **бит4** = порт заливки есть. Кэш возможностей сбрасывать при смене ядра.
  - `MACHINE_CFG` (0xBC): **бит8 = трап TR-DOS, бит9 = сервисная страница.** Биты 0..6 занимать нельзя —
    у NES там region/palette/sprlimit того же слова. Чтение 0xBC теперь отдаёт ЦЕЛОЕ слово.
  - **`$readmemh("rom128.hex")` остаётся дефолтом** — без файлов на карте плата поднимается. НО:
    залитое ПЗУ **не восстанавливает ни F11, ни RESET из меню** (вайп трогает только ОЗУ) — только
    перезагрузка ядра через PCAP (`pl_reload`, в меню это выбор `ROM SET = BUILT-IN`).
  - **`mem_zx.v` — ОБЩИЙ файл**, его копируют себе assemble.sh шагов 06..14, где топ инстанцирует его
    БЕЗ параметра. Поэтому `parameter ROM_PAGES = 2` (старое поведение) — дефолт, а 4 страницы шаг 15
    запрашивает ЯВНО. Менять дефолт нельзя: у тех деревьев своя страничность и ПЗУ уедет в нули.
  - Трап Beta Disk: M1 по `0x3Dxx` при вставленной странице 48 BASIC → страница 2; выход — M1 вне окна
    ПЗУ. Гейт ОБНУЛЯЕТ защёлку (иначе снятие бита8 при вставленном TR-DOS морозит окно на странице 2).
  - **Подпись TR-DOS обязана быть строгой — `"TR-DOS Ver"`**: слово `TR-DOS` есть и в сервисных ПЗУ
    (сообщения «No disk»), по слабой подписи в слот TR-DOS уезжает сервисная страница.
  - `MACH_DBG` (0x150) у ZX теперь: `{запросов в DDR[31:24], страница ПЗУ[23:22], EFF7[21:14],
    DOS[13], 0[12], 7FFD[11:6], банк[5:0]}`. Защёлку DOS опросом не поймать (у промпта TR-DOS
    исполняется 48 BASIC) — доказывать содержимым экрана.
  - На старте прошивка СЛЕДУЕТ за прошитым в BOOT.BIN ядром (`loader_main.c` ~8016) — для проверок ZX
    нужен ЗАГРУЗОЧНЫЙ ОБРАЗ С ZX-ЯДРОМ, иначе `defmachine=pent1024` молча исправится на NES.
  - Сервисное ПЗУ Gluk на холодном старте экран НЕ рисует (его вход — NMI/«магия»); из холодного
    старта работают только образы вида ZX Diagnostics.
  - **NMI проверять ТОЛЬКО на `PENTDIAG.ROM`** (`SERVICE ROM = YES` → сброс → `Ctrl+Alt+Ins` даёт тест
    клавиатуры; без NMI — обычный тест). Gluk для этого не годится: его сервисная страница лезет к
    CMOS-часам Mr Gluk по портам с младшим байтом `#F7` (`0x11AE LD BC,#DFF7 : OUT (C),H`, плюс
    `#0FF7`/`#3FF7`), а у нас из всех `#xxF7` декодируется только `EFF7`, и то по `A15..A12=1110`,
    `A3=0`. Чтения возвращают плавающую шину → мусор в верхней строке, где у Gluk статус и часы.
    У `PENT611Q` страница 0 такая же: четыре прямых `IN/OUT (#F7)` в никуда. Часов у нас нет вообще
    (у Zynq нет батарейки) — это отдельная фича, а не поломка ПЗУ.
  - **NMI в фабрике — только импульс на вход Z80** (`bulbulator_zx_ddr_top.v:190`), страницу ПЗУ он не
    меняет. Поэтому в 128-меню и 48 BASIC он попадает в заглушку на `0x0066` и выглядит как «просто
    перезагрузка». Настоящая магическая кнопка = вставить сервисную страницу по M1 на `0x0066` и снять
    по опкоду `RETN` (`ED 45`), как это делает Multiface. Правило трапа TR-DOS («снять по M1 вне окна
    ПЗУ») здесь НЕ работает: фризер и возвращается в ПЗУ, флаг не снялся бы никогда.
- **Приём «читать текст с экрана по шрифту из `rom128.hex`» верен, только пока в странице 1 стоит
  48 BASIC.** После заливки чужого набора шрифт брать из файла набора (смещение страницы 1 + `0x3D00`).
- **🥇 ОЖИВЛЕНИЕ ПЛАТЫ, когда ядра исчезли из JTAG (без снятия питания).** Признак: `targets` отдаёт
  только `DAP (APB AP transaction error, DAP status 0x30000021)` и `xc7z010`, Cortex-ов нет, память
  через DAP не читается («Context does not support memory read»), то есть и QUIESCE не записать.
  Помогает **`rst -system`, поданный на цель `DAP`** → ядра возвращаются, BootROM берёт образ с карты
  (`sources/jtag_revive_dap.tcl`). НЕ работают: `rst -por` («por not supported for target» — на
  Zynq-7000 через xsdb POR недоступен) и `rst -system` на цели `xc7z010` («Invalid reset type»).
  Пропала вся цепочка — сначала поднять сервер (он в Vivado_Lab, не в Vitis):
  `/tools/Xilinx/Vivado_Lab/2023.1/bin/hw_server -s tcp::3121`. Это НЕ лечение полуоткрытого бёрста
  в S_AXI_HP0 (замёрзший кадр при живом звуке) — там по-прежнему нужен настоящий POR, другой отказ.
- **Тёплая загрузка прошивки по JTAG (`rst -system` + `ps7_init` + `dow`) НЕНАДЁЖНА:** второй раз
  `dow` сорвался и утащил за собой отладочный APB (см. выше). Прошивку проверять **с карты**: собрать
  образ (`flash/mkboot_zx.sh <bit> <out>`), записать мейлбоксом (`put_card.tcl`), тёплая перезагрузка
  (`sources/warm_sd_reboot.tcl`).
- **🥇 ПЗУ ПРОВЕРЯТЬ БАЙТАМИ, А НЕ ПОДПИСЬЮ НА ЭКРАНЕ (оплачено полднём поисков не там).**
  Наш `PENT128_504.ROM` (и `ZX128_504.ROM`, и `TRDOS504.ROM`) — это НЕ TR-DOS 5.04, а **испорченный
  дамп 5.03** с переписанной строкой версии: 17 битых байт. Один из них, `0x1BB7` в странице TR-DOS
  (`34 INC (HL)` → `30 JR NC`), убивает запись сектора каталога БЕЗ сообщения об ошибке: пропадает
  `PUSH HL`, поэтому `POP HL` снимает со стека адрес возврата и переход на запись дескриптора
  (`JP 0x0569`) не исполняется. Симптом: `SAVE` проходит, данные и служебный сектор пишутся, каталог
  пуст, «OK» НЕ печатается. Починенные файлы на карте: `0:/ROMS/PENT504F.ROM`, `ZX128504F.ROM`.
  **Признак гнилого дампа:** одиночные не-`FF` байты внутри длинных прогонов `FF` в неиспользуемых
  областях (у нас пять таких). Настоящая 5.04T отличается от 5.03 в 443 байтах (турбо-формат, фикс
  HL′, скорости шага), а её `SAVE` бит-в-бит равен 5.03 — то есть особых требований к контроллеру у
  «5.04» нет и быть не может. Прочие сломанные байты того же дампа: файл №17+ вешает каталог
  (`0x17F6`), `ERASE` пишет мусор (`0x07EA`), а цикл ожидания DRQ на каждом опросе пишет байт в буфер
  (`0x3FDE`) — отсюда и мусор в служебном блоке.
- **🥇 СВЕРЯТЬСЯ С ЖИВЫМ АПСТРИМОМ, А НЕ С ВЕНДОРЕННОЙ КОПИЕЙ.** Копия `wd1793.sv` в
  `cores/zx-mister/` СТАРШЕ живого MiSTer, и нужной строки в ней уже нет. Дефект геометрии
  (`{ra_sector, wdreg_sector} <= {din,din}` — процессор задаёт и номер для Read Address) нашёлся за
  минуты по `raw.githubusercontent.com/MiSTer-devel/ZX-Spectrum_MiSTer/master/rtl/wd1793.sv`.
- **🥇 НЕ ОТДАВАТЬ В ФАЙЛ ВЕСЬ БУФЕР КОНТРОЛЬЕРА.** Писать только те байты, которые реально записала
  машина (фабрика отдаёт диапазон `cpu_min`/`cpu_max`, ARM читает блок из файла и накладывает их).
  «Непрокрытая» часть буфера принадлежит ДРУГОЙ операции: запросу на отдачу сектора не всегда
  предшествует подача того же блока, и хвост чужого сектора затирал тип диска, число свободных
  секторов и метку. Признак фантомного запроса: **`FDCS_WR` поднят при снятом `FDCS_BUSY`** —
  настоящий запрос всегда приходит при поднятой занятости.
  **Не требовать «этот LBA только что читали» (`g_push_lba == lba_w`) как условие записи.**
  Оплачено в v0.15.416: `WRITE TRACK` (FORMAT) нули пишет, а читать сектор заранее не обязан —
  запрет оставлял каталог как был при «успешном» FORMAT. Сторож чужого хвоста — диапазон
  `cpu_min`/`cpu_max` и свежее чтение файла, не совпадение с последней подачей.
- **`.mkdir` 36 с: Z80 ждал ACK каждого сектора, а не «долгий f_write». ** Фабрика в `S_RXACK`
  не отдаёт data-response, пока ARM не подтвердит WRACK. Снимать `f_sync` мало; очередь 422 тоже
  мало, если насос зовётся **раз за проход цикла ПОСЛЕ `gs_service`** (до 134 мс звука). esxDOS
  пишет сектор, ждёт ACK, пишет следующий — в FPGA пачка не копится никогда. Правило одно на оба
  конца: держать насос, пока карта просит, и ещё ~20 мс после последнего запроса (v0.15.423).
  Порог сброса очереди на SD обязан быть **дольше паузы между секторами кластера** (100 мс, не 5:
  иначе `f_write` снова встаёт на пути ACK).
- **🥇 `f_lseek` ЗА конец образа УДЛИНЯЕТ файл** (FatFs, файл открыт `FA_WRITE`). Оплачено 21.08:
  `test.TRD` 640 КБ вырос в 1024 КБ — это потолок 20-битного `img_size` контроллера, не геометрия
  TR-DOS. Любой ход к образу (чтение и запись) обязан оставаться внутри размера, защёлкнутого при
  монтировании; хвост длиннее 640 КБ у TRD обрезать при вставке.
- **Окно `sd_ack` обязано быть шире ~600 нс:** контроллер сдвигает ack по такту разрешения (период
  ~285 нс), а две подряд идущие записи `FDC_CTL` дают окно ~50-200 нс — оно теряется целиком, и
  запрос не снимается никогда. При этом `sec_cnt` инкрементируется всё равно, то есть прибор врёт.
- **Тайминги дисковода у нас СОФТОВЫЕ, физику вращения эмулировать незачем.** Оборот 10 мс (было 200),
  темп байта ~4.6 мкс (было 32) — иначе софт, ждущий индексные импульсы, идёт в 20 раз медленнее и
  `CAT` выглядит зависшим. Медленный темп сохранён опцией `slow_disk` для трек-загрузчиков в ОЗУ,
  которые когда-то теряли байты.
- **🥇 Ethernet этой платы висит на выводах ПЛИС, а не на MIO.** Живые MIO 16..27 и 52/53 стоят как
  GPIO, такты GEM0 берутся из EMIO (`RCLK_CTRL=0x11`, в `CLK_CTRL` бит6), а в проекте ПЛИС прежнего
  веб-КВМ (`ws/ebaz_kvm/hw/design_4_wrapper.xsa`) есть `C_EN_EMIO_ENET0` и выведенные наружу
  `ENET0_GMII_*` + `ENET0_MDIO_*` в наборе MII. Значит **сеть одной прошивкой не поднять** — нужен
  битстрим, выводящий GEM0 на ноги PHY. Признак отказа: `frames_tx`/`frames_rx` (GEM0 `0x108`/`0x158`)
  нули и ни один из 32 адресов MDIO не отвечает. Мукс MIO на GEM0 из ARM — ТУПИК: провода не
  появляется, а **MIO 24/25 — это UART платы** (`L3_SEL=7`), и правка его ломает. Ног PHY в наших
  файлах не сохранилось (ни `.xdc`, ни в `.xsa`) — брать из документации платы и проверять.
- **🥇 ПРАВКА В ОБЩЕМ ВЕРХНЕМ МОДУЛЕ = ПЕРЕСОБРАТЬ ЯДРА ВСЕХ МАШИН, а не только той, что тестируешь.**
  `bulbulator_zx_ddr_top.v` — один файл на все ZX-машины: `build.tcl` собирает из него Atlas,
  MiSTer-48 и гибрид разными `-tclargs`. Значит правка ленты, варпа, звука или гейтов попадает во все
  машины **только после пересборки каждой**, а на карте лежит ОТДЕЛЬНЫЙ файл на машину
  (`0:/CORES/ATLAS.BIT.BIN`, `MISTER48.BIT.BIN`, `NES.BIT.BIN`). Оплачено 13.08: владелец проверил
  починку пробела на Atlas (работает), переключился на MiSTer-48 — не работает, потому что там лежало
  ядро **B0059**, отставшее на семьдесят сборок. После пересборки (B0131) заработало и там.
  Следствия: у каждой ветки в `bulbulator_zx_ddr_top.v` **свой** `BUILD_VERSION` под `ifdef — поднимать
  надо тот, что в нужной ветке (легко поднять чужой и не заметить); и **смена машины в меню
  перезагружает ПЛИС с КАРТЫ**, поэтому подмена ядра только через PCAP живёт до первого переключения.
- **🥇 ЗАПИСЬ НА КАРТУ: сверять РАЗМЕР ПО КАТАЛОГУ, и делать это ДО перезагрузки чего-либо.**
  Оплачено испорченным `0:/CORES/ATLAS.BIT.BIN` и вынужденной перезагрузкой платы (13.08). Мейлбокс
  отчитался `done=1, err=0`, а по своему же счётчику `n` записал **11947 байт из 1215552**: на карте
  кончилось место, потому что перед этим я положил туда отдельный файл на 1.2 МБ. Ядро на карте стало
  обрезком, а следующая смена машины в меню подняла бы его. Правила на любую заливку файла на карту:
  1) писать во **временный** файл, 2) сверять его размер **листингом каталога** (cmd 1), а не полем
  `n` и тем более не полем `done`, 3) только потом переименовывать (cmd 4) поверх боевого. Тогда при
  нехватке места пострадает временный файл, а рабочий останется цел. Ещё: **следить за свободным
  местом** — каталог `0:/CORES/` копит `.BAK`-и по 1.2 МБ каждый.
- **🥇 РАЗРЕШЕНИЕ ПРОИГРЫВАТЬ ЛЕНТУ — НЕ РАЗРЕШЕНИЕ УСКОРЯТЬ ПРОЦЕССОР (оплачено ШОКом ч.2, B0189).**
  `cpuwarp_req` висел на `tape_streaming`, который пропускает всё НЕпомеченное, а записанная пауза между
  стандартными блоками как раз непомечена (`tape_sync_rom=0`). Значит на паузе процессор шёл 8×. ШОК
  вызывает `USR 62CE` ровно там — меряет длительность кадра и выбирает ветку 48K/128K (байт режима `62BD`:
  30 = 48K): на ускорении он уходил в 128K-ветку, и на экране граница цвета уезжала на 8 px в строке
  (цикл 228 T на растре 224 T) плюс шов в левом бордюре x24/x40. Прежнее правило B0129 «между блоками
  машина идёт в родном темпе» покрывало только ожидание следующего пилота (`sync_hold`), но не паузу.
  Лечение (B0189, только верхний модуль): помнить класс блока через его паузу (`sync_standard_context`) и
  гасить ТОЛЬКО ускорение процессора, не трогая продвижение ленты и не трогая SAFE4/WAV/MP3/кастомные
  загрузчики. **Общий урок: у «лента движется» и «можно ускоряться» должны быть РАЗНЫЕ предикаты** — это
  зеркало старой мины «у включения и выключения латча разные предикаты».
- **🥇 ПРАВИЛО «75 с ТИШИНЫ ПО JTAG» НЕ УНИВЕРСАЛЬНО — считать по САМОЙ ленте.** В
  `SHOCK_PART2_ONLY_g.TAP` 137.95 с импульсов реального темпа (пилот 8063/3223 × 2168 T, sync 667+735,
  бит 855/1710, паузы). При выключенном ускорении нужно ≥155 с; проверка на 90-й секунде дала
  «R Tape loading error, 10:9» и испортила A/B. Время считать по блокам ленты, а не брать из памяти.
- **🥇 МЕЙЛБОКС НЕ ОБСЛУЖИВАЕТСЯ, ПОКА ИДЁТ ЛЕНТА, И КОМАНДА ОСТАЁТСЯ В ОЧЕРЕДИ.**
  `loader_main.c:14971` — `if(!g_tape_on) fs_service();`. Команда, записанная во время загрузки, не
  обнуляет `cmd`, держит `done=0` и срабатывает САМА, когда лента доиграет. Оплачено 16.09: смена ядра
  (cmd 9) «провалилась», а на деле встала в очередь и переключила ядро посреди показа демки. Перед
  cmd 9 / cmd 2 читать `g_tape_feed` (по `arm-none-eabi-nm loader.elf`), а не только `done`.
- **🥇 Варп отпускается по молчанию ЛЕНТЫ, а не по молчанию порта `#FE`** (оплачено SHOCK.TAP,
  B0128). Сторож простоя перевзводился по `smp_edge` — ЛЮБОМУ чтению `#FE`, а демо-часть опрашивает
  клавиатуру раз в кадр (~70000 тактов) при пороге отпускания `0xDC WARP_HOLD` = 0x200000 (~600 мс),
  то есть держала 8× вечно, всю часть. Детектор ЗАХВАТА при этом давно умный (плотный цикл ИЛИ
  корреляция с краями), а сторож ОТПУСКАНИЯ оставался грубым — умный решал, когда включить, а
  выключить не давал никогда. Правило одно на оба конца: признак загрузки = `corr_read` (чтение
  `#FE` ВСЛЕД за краем ленты), и захват дополнительно требует `tape_alive`. **Общий урок: если у
  включения и выключения одного латча разные предикаты, побеждает более грубый.**
- **Пробел, которым софт «продолжает», штатно ломает следующий `LOAD`** — и это НЕ дефект машины.
  `LD-SAMPLE` ПЗУ (0x05ED) читает тот же полуряд `0x7FFE` и тот же бит 0, поэтому зажатый пробел
  рвёт загрузку на любом настоящем Спектруме. Наша вина бывает только в том, что окно на отпускание
  сжато варпом. Считать окно по коду софта: у SHOCK.TAP это два `HALT` и две очистки экрана
  (`LDIR` 6911 байт = 41.5 мс каждая) ≈ 120 мс. Инструмент разбора: `research/15-pentagon/tools/tap_inspect.py`.
- **T80: `IM 2` = 19 тактов (у нас верно), `NMI` = 11.** Два такта ожидания `Auto_Wait` реальный Z80
  вставляет ТОЛЬКО в подтверждение маскируемого прерывания. Наш форк Atlas тянул в то же условие
  `NMICycle` и давал 13 — починено в `sources/t80_bulb/T80.vhd` (B0128), апстримный `zx/src/T80/T80.vhd`
  выкинут из списка чтения `build.tcl` **по имени**. **НЕ «чинить» закомментированные `TStates <= "100"`
  в `T80_MCode.vhd`** (два PUSH цикла IM 2): раскомментировать = получить ровно дефект стока 0242,
  у которого IM 2 стоит 21 такт.

## 🥇 ULA 48K: первоисточники и правила, найденные 08-09.09

- **🥇 ЕСТЬ ПОЛНЫЙ ВЕНТИЛЬНЫЙ НЕТЛИСТ ULA, ВОССТАНОВЛЕННЫЙ ПО ФОТОГРАФИЯМ КРИСТАЛЛА** —
  `github.com/emu-russia/ula` (519 вентилей сверены нетлист↔HDL) плюс книга Chris Smith
  «The ZX Spectrum ULA» (GNU FDL, текст доступен). **Спорные вопросы о поведении ULA решать ПО НИМ,
  а не рассуждением и не по эмуляторам.** Счётчик кристалла C[8:0] 0..447 — это наша `hUla` один
  в один (гашение 320..415, hsync 344..375 совпали).
- **🥇 ЗАЩЁЛКА БОРДЮРА У КРИСТАЛЛА — НА ФАЗЕ 5, А НЕ 4.** Регистр бордюра НЕ квантован вовсе
  (прозрачная защёлка, `nPortWR = nor4(/IOREQ, A0, /WR, nIOREQT2)`); квантование даёт ЕДИНСТВЕННАЯ
  выходная защёлка атрибута `nAOLatch`, активная при `C[2:0] == 5`. Она одна на бордюр И бумагу,
  а мультиплексор стоит ПЕРЕД ней — поэтому бордюр и бумага физически не могут разъехаться.
  То же число независимо вымерил Потапов на живой машине через btime (`BORD_PHASE = 5`).
  ⚠ Прежнее правило «фазу группы не крутить» было выведено на СЛОМАННОМ приборе (ручка добавляла
  вторую точку защёлки, не убирая первую) — СНЯТО.
- **🥇 ЗАПИСЬ В БОРДЮР ПРИМЕНЯЕТСЯ ПОСЛЕ КОНТЕНШЕНА, А НЕ ДО.** Цикл ввода-вывода занимает
  `[s, s+3+d]`, где d — штраф; у настоящей машины положение цвета = КОНЕЦ КОМАНДЫ МИНУС ТРИ = `s+d`
  (правило WoS «48K reference»: OUT, заканчивающийся на 14339..14342, меняет бордюр ровно в позиции
  байта 16384). **FUSE, ZEsarUX и Spectrusty кладут на `s`** — в FUSE это буквально соседние строки
  `periph.c:368-373`. Отсюда чёрная чёрточка 8x1 px слева от бумаги на первой строке экрана в esh2,
  которой на живом 48K нет. Такт **14335** — единственный в строке, где одновременно красится
  последняя группа бордюра и начисляется максимальный штраф 6. Починено в B0174 добавлением
  `contend` к условию защёлки.
- **Опережение контеншена (окно открывается раньше первого видимого пикселя) — НОРМА, а не дефект,
  и у нас оно уже правильное.** FUSE `spectrum.c:203` offset = 1 такт, ZEsarUX `contend.c:1469`
  «Empieza en 14335», JSSpeccy `buildContentionTable(14335,…)`. По кристаллу контендятся счёты
  4..15, 20..31, …, 244..255 — наш `cn = dataEnable && (hUla[3] || hUla[2])` даёт ровно это.
- **🥇 ОКНО КОНТЕНШЕНА ПОРТОВ ДОЛЖНО СТОЯТЬ В НУЛЕ (`io_cont_tap = 0`).** Сдвиг на такт, введённый
  в B0154 ради приёмочного CONTP 14339, уводил вход в HALT и двигал весь код после него на 8 px —
  на экране это верхняя полоса квадратов esh1/esh2, уехавшая вправо. Найдено бисектом по ядрам
  (B0153 верно, B0154 нет) после подсказки владельца «раньше шло правильно». У Потапова `io_base = 0`
  на всех машинах, а прежние 32 counts он называет дефектом и убрал. Цена: CONTP 14338 вместо 14339.
  Прежняя фаза — переключатель `MACHINE_CFG` бит27.
- **🥇 ПЕРВАЯ СТРОКА ЭКРАНА НИЧЕМ НЕ ОСОБЕННА В САМОЙ ULA.** Border, VidEn, AOLatch, SLoad и арбитр
  контеншена — чистые функции счётчиков. Поэтому любой артефакт «только на первой строке» ищи в
  ПРОЦЕССОРНОЙ фазе: это единственная строка, где она ещё не залочена контеншеном предыдущей.
- **Демка esh2_48 разобрана:** грузится на 0x8000, синхронизируется по HALT, красит бордюр
  развёрнутым прогоном `ED 71 / ED 41 / ED 79 / NOP`, строка ровно 224 T, поправки на контеншен нет.
  Поэтому она ДРЕЙФУЕТ по кадру — и настоящая машина дрейфует так же (лесенка вдоль квадратов есть
  и на фото живого 48K). Сдвиг прерывания её не двигает вовсе: выход из HALT квантован по 4 такта.

## 🥇 БОРДЮР ПЕНТАГОНА: ШАГ 2 ПИКСЕЛЯ — НОРМА, 8 — ДЕФЕКТ

Сверено с двумя живыми реализациями (правило «сверяться с ЖИВЫМ апстримом»):
- Sizif-512 `cpld/rtl/video.sv`: `border_update = (hc0[4:0]==5'b10011) || (machine==MACHINE_PENT && ck7)`,
  `ck7 = hc0[0] & hc0[1]` → у Пентагона каждые **2 пикселя**, у остальных раз в восьмипиксельную группу;
- ep4spectrum (Потапов, мерено на живой машине): `border_update = (MACHINE==MACHINE_PENT) ? hcounter[0] : (hcounter[3:0]==BORD_PHASE)`,
  «ULA гонит бордюр прямо из защёлки порта, границы знакоместа на пути нет».
Эталоны расходятся (2 px против 1 px), но НАБЛЮДАЕМО это одно и то же: цвет меняет `OUT (#FE),A`, а её
можно поставить только на границе такта процессора = на чётном пикселе. Значит **шаг края 2 px — пол,
ниже не бывает**; переключатель не нужен. Ступеньки по 8 px = потерянный `borderFine` (было в B0171..B0191,
возвращено в B0192 термом `if(border_upd | borderFine) border_lat <= border;`).

⚠ **Прибор Пентагона — `0:/test/Pentagon/RageEnd.tap`, но он ВРАЩАЕТСЯ.** Сравнивать два кадра между
собой нельзя — получишь поворот, а не сдвиг (оплачено 17.09 ненужной сборкой B0193). Мерить внутри
ОДНОГО кадра: бумага начинается со строки 62, берём строки чуть выше и чуть ниже и сравниваем ту же
границу. Для вопросов «бордюр против бумаги» нужен СТАТИЧЕСКИЙ прибор (своя программа по JTAG:
`OUT (#FE)` на фиксированном такте каждую строку + вертикальная линия в бумаге).
⚠ **Прежде чем чинить сдвиг в ядре — проверить `Pan X`/`paper_h` у машины.** У Пентагона там стояла
двойка (компенсация КВАНТОВАННОГО бордюра), и после возврата попиксельного она стала лишней: ноль
вместо двойки выровнял картинку без правок железа.

## 🥇 ПРАВКА ОБЩЕГО ВИДЕОТРАКТА = ПРОВЕРИТЬ ВСЕ ТРИ МАШИНЫ, А НЕ ТУ, НАД КОТОРОЙ РАБОТАЕШЬ

Оплачено 16.09 потерянным тонким бордюром Пентагона. Хронология: **B0121/B0122** (12.08) сделали
попиксельный бордюр Пентагона, принято на плате. **B0171** (08.09) завёл конвейер задержки бордюра
(`border_lat` → `border_d1..d3` → `border_live`) и переключил вывод цвета на него, а новый регистр
берёт цвет ТОЛЬКО по квантованному стробу `border_upd`. Ветка `borderFine` осталась жива, но стала
обновлять `attrOutput`, который на бордюр больше не влияет. Приёмка правки была честной — «при нуле
0 расходящихся строк» — но проведена на СИНКЛЕРОВСКОЙ машине, где `borderFine = pentagon & ~paperShow`
тождественно ноль. Разницы там не было и быть не могло. Владелец увидел это через неделю на финале
демки Rage (`0:/test/Pentagon/RageEnd.tap`): секторы на бордюре пошли ступеньками по 8 px.

**Системная причина: все наши видеоприборы синклеровские.** Timing Tests, esh2, ula48, попиксельная
сверка кадра — 48K и 128K. У Пентагона автоматического прибора НЕТ, его проверка — глаза владельца.
Значит пентагоновскую ветку можно уронить молча.

Правила:
- Любая правка `atlas_core/video.v`, `main.v`, `fb_capture_rr.v` проверяется на **48K, 128K И Пентагоне**.
  Это зеркало уже действующего правила «правка общего верхнего модуля = пересобрать ядра всех машин».
- **Прибор для Пентагона: `0:/test/Pentagon/RageEnd.tap`** (финал демки Rage — экран из восьми цветных
  секторов, покрывающих и бумагу, и бордюр; рядом лежит `RageEnd.sna` для мгновенной загрузки).
  Метрика объективная: снять кадр (`snap14.py`) и посчитать, сколько СТРОК держится одна координата
  границы сектора в верхнем бордюре. Плавно = 1-2 строки и много разных x; «кубики» = ровно 8 строк и
  все x на сетке 8. У сломанного B0191 измерено: x = 51, 59, 67, 75, держится по 8 строк, а та же
  граница В БУМАГЕ идёт по 1 px на строку.
- ⚠ **У 48K восьмипиксельное квантование бордюра — ПРАВИЛЬНОЕ поведение** (живая ULA берёт цвет раз в
  восемь пикселей; у Потапова это описано словами «a real 48K cannot do that»). Тонкая ветка —
  исключение РОВНО для Пентагона, поэтому её легко потерять, работая над 48K, и нельзя «починить»
  снятием квантования у всех.

## 🥇 Приборы 48K (правила, оплаченные ложными выводами 03.09)

- **🥇 СВИП `probe.asm` С `NT>1` НЕ ДАЁТ ТАБЛИЦУ КОНТЕНШЕНА — мерить только `NT=1`, один прогон на такт.**
  Хвост зонда компенсирует НОМИНАЛЬНЫЙ такт, но не ту задержку, которую только что получила сама
  измерительная запись; вход в `HALT` уезжает, а Z80 в HALT принимает INT по сетке 4 T от момента входа:
  `shift(N+1) = (shift(N) + d(N)) mod 4`. Свип физически пробует только фазы 0, 3 и 7 (mod 8), часть
  повторяет по 3-4 раза и выдаёт ПРАВДОПОДОБНЫЙ МУСОР. По нему я прочитал «6,5,4,3,3,9,1,7» и объявил
  дефектом — неверно. Термометр дрейфа — колонка «1-й доступ» в `tab.py`, номинал `T=72`.
  **Наша таблица контеншена памяти КАНОНИЧЕСКАЯ 6,5,4,3,2,1,0,0**; якорь 14336 (вариант Fuse «late»),
  и только при нём получается принятый `stime` = 14335.
- **🥇 ВИДИМОЕ ОКНО 48K = 352 px, А НЕ 384.** Строка 224 T = 448 px: 128 T бумаги + 24 T правый бордюр +
  **48 T гашения** + 24 T левый; бордюры 48/48, бумага `hUla 12..267`, гашение `hUla 316..411`.
  Расширять окно до 384 НЕЛЬЗЯ: тогда наружу вылезают записи бордюра, которые демки намеренно прячут в
  гашение (на SHOCK ч.2 — 62 записи на кадр). Буфер кадра 384 (`CAP_W`) надо ДОБИВАТЬ цветом бордюра,
  и добивать СИММЕТРИЧНО, иначе картинка уезжает вбок.
- **🥇 ФАЗУ 8-ПИКСЕЛЬНОЙ ГРУППЫ ЗАЩЁЛКИ БОРДЮРА НЕ КРУТИТЬ: защёлка не может опередить сам `OUT`.**
  Свип всех восьми фаз на живой плате даёт ОДНУ И ТУ ЖЕ гистограмму переходов (`x = 5,13,21,29,37`).
  Наша фаза и так совпадает с обоими живыми эталонами (`attrOutputLoad = h_addr[2:0]==4`; MiSTer
  `rtl/ula.sv:175-178`; Sizif `cpld/rtl/video.sv:186`). Если край бордюра виден в растре — значит поздно
  ложится САМА ЗАПИСЬ, и лечится это парой «фаза на такт раньше + пиксельные ступени возврата позиции»
  (у Потапова `BORD_PHASE`/`BORD_DELAY`), а не одной фазой. Цепочки ступеней у нас пока НЕТ.
- **🥇 ТРИ ПРИЁМОЧНЫХ ЧИСЛА СЛЕПЫ К СДВИГУ «ПРОЦЕССОР ПРОТИВ РАСТРА».** `stime`, READP и CONTP считаются
  ОТ ПРЕРЫВАНИЯ, поэтому общий сдвиг им невидим, а демки его видят. Приёмка по демкам — отдельная:
  метрика по честному кадру (cmd 14) «сколько переходов цвета внутри левого и правого бордюров и сколько
  строк с левый ≠ правый»; у эталона 0 и 0..3.
- **Timing Tests 128K: набор кончается на тесте 34, а приглашение врёт.** Оно печатает
  `choose test 1-35` (текст унаследован от 48K-версии), но строка 1350 (= тест 35) содержит `STOP`:
  выбор теста 35 вручную даёт `9 STOP statement, 1350:1`, и полный прогон кончается тем же. Тесты
  портов и плавающей шины (35/36/37) есть ТОЛЬКО в 48K-версии. Строки «All Tests Complete» у 128K
  не бывает — концом служит `STOP`. Ещё: 128K-тест требует режим **48 BASIC** (из 128-меню), иначе
  печатает `** MUST RUN IN 48k MODE **`; наш автостарт ленты жмёт ENTER на 128-меню и попадает в
  «Tape Loader», то есть в 128 BASIC — для этого теста машину надо вести вручную.
- **🥇 ТЕСТЫ 35, 36 и 37 Timing Tests ЗАВИСЯТ ОТ СОДЕРЖИМОГО ЭКРАНА — мерить ТОЛЬКО полным прогоном со
  свежей загрузки (оплачено 16.09 ложным диагнозом «дефект плавающей шины»).** Они читают плавающую шину,
  а она отдаёт БАЙТЫ ЭКРАНА; тело делает `IN r,(C)`/`OUT (C),r`, где старший байт адреса порта — это байт
  с шины. Поэтому: пошаговый обход по «Press any key», одиночные тесты и повторный запуск после частичного
  прогона ломают их на исправном железе. Каскад: любой провал ДО 35-го печатает строки «Expecting:», экран
  прокручивается иначе — и 35/36/37 валятся следом (так было 11.09 на B0181). **Стенд xsim для этих трёх
  тестов не годится вовсе** — в его образе свой экран (`t35_1.bin` и `t36_1.bin` различаются 2496 байтами,
  все в экране 0x4120..0x4EBF), и он их «проходит» независимо от железа. Правильный прогон: свежая лента →
  95 с без JTAG → тест идёт весь сам → собирать `/tmp/walk.py` → вердикт по строке «All Tests Complete
  100% Pass».
- **Прибор по контеншену — `ZX-Soft/tests/ttst48_my.tap` (Timing Tests 48K), на карте `0:/test/TIMING48.TAP`:**
  36 тестов по классам команд с самопроверкой Pass/Fail. На приглашении «choose test 1-35 or leave blank»
  жать просто ENTER. **`L` в кавычках — курсор BASIC INPUT, не буква:** стирать Backspace нельзя
  (сносит кавычку; оплачено 11.09). Набрать номер и Enter. OCR `"35L"` = «35» плюс глиф курсора.
  Результаты — периодическим OCR (`tools/frame/ttpoll.py` + `zocr.py`). На B0184 (`irqBeg` 4/8)
  занятый код 2/3/17/18/22/27 и RST 18 проходят; остаток 35_0/36_1/37_1 — плавающая шина, не INT.
- **ZEsarUX — эталон ПОВЕДЕНИЯ демок, НЕ источник одиночных тактов.** `stime` у него 14335 (= живая
  машина), но READP/CONTP на такт РАНЬШЕ машины, и он валит ровно Test 36 Timing Tests (контеншен
  портов); процессорные тесты тоже валит. Проверять им «попала запись в гашение или нет» — можно,
  сверять такты — нельзя. Протокол его прогона: `tools/frame/tt_ref.txt`.
- **Снапшот `.SNA` в уже запущенный ZEsarUX через ZRCP `smartload` НЕ поднимается** (машина уходит в ПЗУ,
  `PC=0038`, `I=3F`); ленты `.TAP` поднимаются штатно. То же и на плате — владелец грузит только ленты.
  Для стенда `.SNA` годится (образ памяти + заглушка регистров, `tools/frame/shock_stub.py`).
- **Сдвинуть ВСЮ демку по тактам, не трогая её код:** вести вектор IM2 (`FEFF/FF00`) на `FFFF-k` — там
  NOP-slide до `RET` на `FFFF`, то есть `+4k` T на кадр (`tools/frame/sweep.py`). Так меряется запас
  демки по таймингу: у SHOCK ч.2 уже +8 T ломают одну строку, +16 T — пятьдесят одну.
- **Любой НОЛЬ в метрике по кадру перепроверять вторым снимком с подсчётом ЦВЕТОВ** — пустой снимок
  `snap14` выглядит как идеальный результат. Признак живого кадра: 8 цветов в бордюре и активные
  столбцы 0..383.

## 🥇 Приборы (правила, оплаченные ложными выводами 19-20.08)

- **ПРИБОР, КОТОРЫЙ МЕНЯЕТ ПОВЕДЕНИЕ ИЗМЕРЯЕМОГО, ОБЯЗАН БЫТЬ ОТКЛЮЧАЕМЫМ, и по умолчанию выключен.**
  Диагностическая трасса GS писала каждое событие протокола в НЕКЭШИРУЕМУЮ память мейлбокса прямо в
  горячем цикле эмуляции карты — и обрывала треки в Z-Player. Доказано зависимостью от дозы: узкий
  фильтр опроса → обрыв через доли секунды, широкий → «играет намного дольше», трасса выключена → трек
  целиком. Флаг `gs_trc_en` (`KMB+0x4408`): бит0 трасса, бит1 снимок данных; умолчание 0.
- **🥇 ВО ВРЕМЯ ЗАГРУЗКИ ЛЕНТЫ К ПЛАТЕ НЕ ПРИКАСАТЬСЯ — ТА ЖЕ БОЛЕЗНЬ, ЧТО У GS (оплачено 11.09 ложным
  «подлинное ПЗУ ломает загрузку»).** Лента кормится насосом ARM в реальном времени; чтение зеркала экрана по
  JTAG раз в 2 с (1728 слов `mrd`) во время загрузки давало «R Tape loading error» на 43 % дважды подряд, а те же
  файл, ПЗУ и ядро при 75 с тишины по JTAG загрузились до приглашения. Владелец видел это и раньше: «загрузка
  не идёт только когда ты что-то делаешь на плате». Правило: ENTER на ленте → ни одного обращения по JTAG до
  конца загрузки (для TIMING48 хватает 75 с) → один снимок. Скрипт: ThinkPad `/tmp/load_quiet.sh`. Сами
  Timing Tests опроса не боятся — они не зависят от ARM (результаты сходятся с оракулом тест в тест).
- **ВО ВРЕМЯ ПРОСЛУШИВАНИЯ МУЗЫКИ К ПЛАТЕ НЕ ПРИКАСАТЬСЯ.** Опрос памяти по JTAG раз в 200 мс сам
  обрывает трек. Для этого в прошивке сделаны ЛИПКИЕ копии худших значений (`gs_s_lgap` и соседние):
  один замер ПОСЛЕ прогона, а не поллинг во время.
- **Чтение `0x180 GS_RQ` ИЗВЛЕКАЕТ байт из очереди** (как клавиатурное FIFO — написано в его же
  описании). Три чтения украли три байта и дали «GS не найден». В приборы этот регистр не брать.
- **Слово позиции модуля GS разбирать ПО БАЙТАМ:** `0x415A` = строка в паттерне (0..63),
  `0x415B` = позиция в песне. Сложение двух байт в одно число даёт ложные «смены трека».
- **Мгновенное значение защёлки не отвечает на вопрос «срабатывала ли она».** Защёлка DOS уходит на
  первом же M1 за окном ПЗУ, поэтому опрос почти всегда попадает ПОСЛЕ снятия. Для этого есть
  `0x1BC ROM_DBG` = {взводов трапа, снятий, адрес последнего взвода} + липкий бит12 в `MACH_DBG`
  («слот 3 был в окне»). Счётчики насыщаются на 255.

## 🥇 Конус джойстика Kempston накрывает ПОЛОВИНУ портов

`ioDF = !(!iorq && !a[5])` — одно условие, без других адресных бит, как у оригинального интерфейса.
Следствие: **любое устройство на порту с `a[5]=0` обязано стоять в мультиплексоре шины ВЫШЕ джойстика.**
Уже пострадали дважды: порт `#57` Z-Controller (чтение крал джойстик, починено в B0144 — карта видела
команды, а ответов машина не видела никогда) и адреса мыши `#FADF/#FBDF/#FFDF` (при ВЫКЛЮЧЕННОЙ мыши
чтение кнопок проваливается в джойстик, тот отдаёт 0, а кнопки активно-низкие = «нажаты все три»).
В коде у NEMO-IDE, мыши и дисковода стоят комментарии «СТРОГО раньше kempston» — читать их.

## 🥇 Страница ПЗУ у настоящего Пентагона = пара {DOS, 7FFD[4]}

У 64-КБ BIOS блок ПЗУ адресуется парой при порядке блоков `[сервис, TR-DOS, 128, 48]`, то есть в нашей
нумерации слотов `{DOS=1, 7FFD[4]=1}` = TR-DOS, а **`{DOS=1, 7FFD[4]=0}` = СЕРВИСНАЯ страница** — этим
штатные BIOS (PentoGraf: FATALL, Proteus) входят в свой файловый менеджер. Подтверждено живыми
Karabas-Pro (`rom_page <= (not TRDOS) & ROM_BANK`) и ZX-Evo (`{~dos, rom128}`) и самими дампами.
У нас это **опция машины** `Service page under TR-DOS` (бит25 `MACHINE_CFG`), умолчание AUTO: бит
поднимается только когда в слоте 3 найден механизм входа (12 нулей по `0x3FF0` + `F3 C3` по `0x3FFC`) —
у двухстраничных наборов и заводского ПЗУ слота 3 нет, и включение увело бы окно в незаписанную BRAM.
**Проверять пару можно только кодом, который остаётся В ОКНЕ ПЗУ:** тест, возвращающийся в ОЗУ, сам
снимает защёлку и врёт.

## Две клавиши сброса: F11 железная, Ctrl+Alt+Del мягкая

Разделение живёт В ФАБРИКЕ (`hard_combo = f11_h` с очисткой ОЗУ, `soft_combo = ctrl&alt&del` с её
сохранением). Виртуальные приборы живут в ПРОШИВКЕ, и фабрика до них не достаёт: на F11 служба
`hard_reset_service` перезапускает карту General Sound, на Ctrl+Alt+Del виртуальные приборы НЕ трогаются
намеренно. Добавлять что-то в железный сброс — только через эту службу, а не из обработчика клавиши
(`gs_boot()` читает ПЗУ карты с SD, а обработчик зовут из любого контекста).

## Том SD: FAT32 по умолчанию, FAT16 — только под Bob Fossil

FAT16 был выбран по НАШЕЙ простоте и трижды выставил цену (предел записей корня; невидимость для чужих
FAT32-драйверов — Wild Player и Z-Player; вис FATALL 0.25 из ПЗУ). На FAT32 esxDOS читает ФС наравне
(замер: 53/42 против 51/40 команд/чтений). **FAT16 нужен ровно одному** — LFN-браузеру Bob Fossil: он
разбирает FAT сам и читает `RootEntCnt` буквально, требуя ровно 512, а у FAT32 этого поля нет вовсе.

## Софт под Z-Controller лежит в `0:/zc/`

21 файл, всё умеет работать с картой по стандарту KOE: FATALL 0.26/0.25, WDC 1.62, Proteus 2.31,
Demeter 1.20, Wild Player 0.333, Z-Player 4.1/4.0, ZX MIDI Player 3, mini_fat_boot, исходники драйверов
SD(ZC), страницы ПЗУ (`PROTEUS.ROM` = 65536 = готовый набор нашего формата). Опись — `0:/zc/README.TXT`.
Спека интерфейса, включая ОРИГИНАЛЬНОЕ описание KOE 2007 со схемой — `EBAZ4205/ZCONTROLLER_SPEC.md`.
У большинства программ драйвер выбирается ПРИ ЗАПУСКЕ — надо указать SD(ZC). **Z-Player управляется
МЫШЬЮ** (в его описании «Kempston Mouse рекомендуется», списка клавиш нет): для управления включить
`Kempston mouse = KEYPAD`, тогда 8/2/4/6 водят курсор, KP0/KP5 левая кнопка, KP-Enter/KP-точка правая.

## 🥇 У КАЖДОЙ ВЕТВИ СВОЙ `BUILD_VERSION`, И ОН ВРЁТ, ПОКА ЕГО НЕ ПОДНЯЛИ

В `bulbulator_zx_ddr_top.v` константа версии стоит под `ifdef` по одной на ветвь: `HYBRID_CORE`,
`MISTER48_CORE`, `MISTER_T80_AB` и `else` (Atlas). Поднимать надо ТУ, которую собираешь.
Оплачено 20.08: я собрал MiSTer-48 со всеми правками и залил на карту, но поднял только атласовский
номер — владелец переключился на MiSTer-48 и увидел «v0.15.396 / **B0059**», то есть ядро новое, а
рапортует шестидесятой сборкой. Пока номер врёт, приборная проверка «что залито» на этой машине
невозможна, а она у нас основной способ убедиться, что на карте лежит собранное.
Симметричная ловушка: **машины 48K/128K Atlas живут на ТОМ ЖЕ битстриме Atlas**, поэтому показывают
атласовский номер — по нему нельзя судить о ядре MiSTer-48, у него свой файл `MISTER48.BIT.BIN`.

## ✅ Ветви MiSTer-48 и гибрида: не собирались с 14.08, ПОЧИНЕНО 20.08 (B0148)

Связи `.zc_en`, `.zc_turbo`, `.dm_opt`, `.dm_pagein_off` стояли в общем топе ВНЕ `ifndef`, а таких
портов у `mister48_core`/`hybrid_zx_core` нет — обе ветви падали на синтезе, и падали МОЛЧА шесть дней,
потому что собирали только Atlas. Убрано под `ifndef`; собраны все три ветви (Atlas 84.5 %,
MiSTer-48 63.6 %, гибрид 89.9 %, у всех тайминг закрыт), `0:/CORES/MISTER48.BIT.BIN` на карте обновлён.
**Правило на будущее: правку общего верхнего модуля проверять сборкой ВСЕХ ветвей, а не той, что
тестируешь.** Это третий случай этой мины за два дня; первые два — порт `#57` и адреса мыши.

## Память проекта: волт Obsidian первичен

Волт: `~/Documents/Obsidian Vault/Projects/Lichnoe/BulbuLator/`. Порядок обновления после каждого шага
(требование владельца — держать в синхроне, волт в первую очередь):

⚠ **Владелец повторил это требование 20.08 отдельной просьбой: «очень важно не забывать держать
обсидиан в актуальном состоянии».** Значит волт обновляется НЕ в конце работ, а по ходу: после каждой
принятой правки, каждой приборной находки и каждого опровергнутого подозрения. Опровержения писать
наравне с победами — они экономят следующему сессии не меньше. Наслаивать новые шапки в `STATUS.md`
поверх старых НЕ надо: свести в один текущий срез, прежние снести ниже в раздел истории.

1. **Волт** — `STATUS.md` (читать первым), `state/current.md` (журнал, новое сверху), `BulbuLator-Kanban.md`
   (планы), тематические заметки в `state/`. Протокол и карта кода: `state/CODE_MAP_AND_SYNC.md`.
2. **Хэндовер на ThinkPad** — `research/15-pentagon/HANDOVER_CURRENT.md`: операционные детали (точные адреса,
   команды, состояние SD/платы), коммитится в `master`.
3. **Этот файл** — только постоянные правила и гочи, которые нужны ВСЕГДА (его видят и подагенты).
4. **Память ИИ-агента** — короткие указатели на 1-3, не дубль содержания.

Расхождение волта и кода: волт — центр правды по *состоянию*, код — по *фактам*. Заметил расхождение —
озвучить владельцу и синхронизировать, а не выбирать молча.

## Прочее

- Тяжёлое чтение/сборки на ThinkPad можно отдавать вспомогательный агент (API `127.0.0.1:8642`), чтобы не жечь контекст.
  У вспомогательный агент прописан этот же codebase-memory MCP — ему можно поручать вопросы по коду прямо на машине-истине.
